	title 'SANE         FP816         GS ROM 3.0'
****************************************************************
*			   *
*	   SANE FP816		   *
*			   *
****************************************************************


****************************************************************
*			   *
*	Copyright (C) 1985-1988	   *
*	All Rights Reserved		   *
*	Apple Computer, Inc.	   *
*			   *
****************************************************************



**********************************************************************
*
* Change History
*
* 12 Dec 88	Kenton Hanson
*
* Fixed Nan codes (NAN comp and NAN zero).
* Fixed conversion error for large comps to exended (in Unpack).
* Fixed error signaling error in Floating point NAN converting to
*       COMP NAN.
*
**********************************************************************

	string asis
	blanks off

	include 'all.macros'
	include '::sys.equs.asm'
	include 'sane.macros'

SaneVersionNumber	equ $0300
True	equ $FFFF
False	equ $0000

;-----------------------------------------------
;
;   Imported addresses
;
;-----------------------------------------------

	IMPORT oEndCall0
	IMPORT oEndCall2
	IMPORT Elems816

;-----------------------------------------------
;
;   Forward addresses and entries
;
;-----------------------------------------------
	ENTRY AtoC
	ENTRY BtoA
	ENTRY BtoC
	ENTRY BtoE
	ENTRY BtoX
	ENTRY CtoB
	ENTRY CtoX
	ENTRY EtoA
	ENTRY EtoB
	ENTRY XtoA
	ENTRY XtoB

	ENTRY CStr2Dec
	ENTRY Dec2Str
	ENTRY DecStr
	ENTRY DnrmC
	ENTRY Eight16
	ENTRY NrmlzC
	ENTRY Rnd2Int
	ENTRY SaneShutdown
	ENTRY SaneStartup
	ENTRY SaneStatus
	ENTRY SaneVersion
	ENTRY ScaleIt
	ENTRY Str2Dec
	ENTRY TrpHd1
	ENTRY UnPck
	ENTRY XitFP
	ENTRY add
	ENTRY ck4URO
	ENTRY mul
	ENTRY pck
	ENTRY rtnA
	ENTRY rtnInf
	ENTRY setXcpn
	ENTRY sqrt
	ENTRY sub
	ENTRY B2D
	ENTRY D2B
	ENTRY Xdiv
	ENTRY REM

;                copy sane/oequs
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; File:  FPEques
;; Description: Equs for ORCA SANE 65816 floating point engine.
;; Status: Alpha
;;
;; Written by Kenton Hanson, Apple Numerics Group, 12 May 1986
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
ZPrtn	equ 0
OrigDirect	equ ZPrtn+6
OrigBank	equ OrigDirect+2
base1	equ OrigBank+2	;10
opword	equ base1
CRgstr	equ base1+2
BRgstr	equ base1+6
ARgstr	equ base1+10
HaltVect	equ base1+14
EnvWrd	equ base1+18
tmpFlgs	equ base1+20
pendingX	equ base1+22
pendingY	equ base1+24
TmpZP	equ base1+26	;36
TmpZP2	equ TmpZP+2
TmpZP3	equ TmpZP+4
TmpZP4	equ TmpZP+6
TmpZP5	equ TmpZP+8
fence	equ TmpZP+10
fence2	equ TmpZP+12
fenceSV	equ TmpZP+14
SBWord	equ TmpZP+16	;Max (Sticky Bit Word, length 2nd argument)
opcode	equ TmpZP+18
ZeroWord	equ TmpZP+20
atag	equ TmpZP+22	;identification tag	            (word)	58
signA	equ atag+2	;sign	            (word)
expnA	equ atag+4	;unbiased exponent	            (Long)
lgthA	equ atag+8	;length of Fraction	in bytes    (word)
btag	equ atag+10	;identification tag	            (word)
signB	equ btag+2	;sign	            (word)
expnB	equ btag+4	;unbiased exponent	            (Long)
lgthB	equ btag+8	;length of Fraction	in bytes    (word)
ctag	equ atag+20	;identification tag	            (word)
signC	equ ctag+2	;sign	            (word)
expnC	equ ctag+4	;unbiased exponent	            (Long)
lgthC	equ ctag+8	;length of Fraction	in bytes    (word)
FrctA	equ atag+30	; 88
FrctB	equ FrctA+26
FrctC	equ FrctA+52
len	equ FrctA+78	; 166
ilog	equ len+2
iscale	equ len+4
style	equ len+6
digits	equ len+8
etag	equ len+10	;identification tag	            (word)	176
signE	equ etag+2	;sign	            (word)
expnE	equ etag+4	;unbiased exponent	            (Long)
lgthE	equ etag+8	;length of Fraction	in bytes    (word)
xtag	equ len+20	;identification tag	            (word)
signX	equ xtag+2	;sign	            (word)
expnX	equ xtag+4	;unbiased exponent	            (Long)
lgthX	equ xtag+8	;length of Fraction	in bytes    (word)
FrctE	equ len+30	;fraction with binary point after first	bit
FrctX	equ len+56	;fraction with binary point after first	bit
ffExt	equ 0
ffDbl	equ 2
ffSgl	equ 4
ffLng	equ 6
ffInt	equ 8
ffComp	equ 10
ffq	equ 12
ffqq	equ 14
DnrmTag	equ 1
ZeroTag	equ -1
InfTag	equ -2
NanTag	equ -3
SNanTag	equ -4
invxcp	equ 01
unfxcp	equ 02
ovfxcp	equ 04
dvzxcp	equ 08
inexcp	equ $10
NanSqrt	equ $4001
NanAdd	equ $4002
NanDiv	equ $4004
NanMlt	equ $4008
NanRem	equ $4009
NaNAscB	equ $4011
NaNComp	equ $4014	; error codes corrected 12 Dec 88 by Kenton
NaNZero	equ $4015	; ibid
BgnSgDg	equ 4
SigDigLn	equ 28	;28 ???

;          copy sane/call.table
	EXPORT SaneCallTable
SaneCallTable	PROC

	DC.L (TheEnd-SaneCallTable)/4
	DC.L oEndCall0-1	; jumps direct 29 dec 86
	DC.L SaneStartup-1
	DC.L SaneShutdown-1
	DC.L SaneVersion-1
	DC.L oEndCall0-1	; jumps direct 29 dec 86
	DC.L SaneStatus-1	; added 29 dec 86 by SEG
	DC.L oEndCall0-1
	DC.L oEndCall0-1

	DC.L Eight16-1
	DC.L DecStr-1
	DC.L Elems816-1

TheEnd		; 

	ENDP
;          copy sane/init.calls
*****************************************************************
*
* SaneBootInit
*
* Since this does nothing, I put the address of oEndCall0
* in the call table.
*
*****************************************************************


*****************************************************************
*
* SaneStartup
*
*****************************************************************
	EXPORT SaneStartup
SaneStartup	PROC
RTL1	equ 1
RTL2	equ RTL1+3
ZPToUse	equ RTL2+3
	lda ZPToUse,s
	phd	; temp store current d
	tcd	; set d to SANE value
	stz HaltVect	; set default env and HV
	stz HaltVect+2
	stz EnvWrd
	pld	; restore current d
	PushWord #0	; system call
	PushWord #10	; tool set number
	PushWord #0	; High byte of WAP
	pha
	_SetWAP

	jml oEndCall2

	ENDP

*****************************************************************
*
* SaneShutdown
*
*****************************************************************
	EXPORT SaneShutdown
SaneShutdown	PROC

	PushWord #0
	PushWord #10
	PushLong #0
	_SetWAP


	jml oEndCall0

	ENDP

*****************************************************************
*
* SaneVersion
*
* Uses global version number equate.
*
*****************************************************************
	EXPORT SaneVersion
SaneVersion	PROC
RTL1	equ 1
RTL2	equ RTL1+3
VersionInfo	equ RTL2+3


	lda #SaneVersionNumber


	EXPORT ShareSTATUS
ShareSTATUS		;       
	sta VersionInfo,s
	jml oEndCall0

	ENDP


*****************************************************************
*
* SaneSTATUS
*
* Added by SEG on 29 Dec 86
*
*****************************************************************
	EXPORT SaneStatus
SaneStatus	PROC

	cmp #0
	beq ShareStatus
	lda #True
	bra ShareStatus

	ENDP


*****************************************************************
*
* SaneReset
*
* Since this does nothing, I put the address of oEndCall0
* in the call table.
*
*****************************************************************
;          copy sane/dt
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; File:  DT
;; Description: Data Constants for ORCA	SANE 65816 floating	point engine
;; Status: Alpha
;;
;; Written by Kenton Hanson, Apple Numerics Group, 13 May 1986
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
	EXPORT DT
DT	PROC
	EXPORT lastW
lastW	DC.W 8,6,2,2,0,6,24,12 ;ext, dbl, sgl, Lng, int, comp, q, q*
	EXPORT lastSig
lastSig	DC.W 6,6,2,2,0,6,22,10
	EXPORT StkBtWd
StkBtWd	DC.W 8,6,2,4,2,8,24,12
	EXPORT ShftDS
ShftDS	DC.W 4,7	;right bit shift for word align Dbl+Sgl
	EXPORT ExpAdj
ExpAdj	DC.W 15360,16256	;2^14 - 2^10, 2^14 - 2^7
	EXPORT MaxExp
MaxExp	DC.W -1,2047,255
	EXPORT MinExp
MinExp	DC.W 0,15361,16257,16414,16398,16446,0
	EXPORT MaxShft
MaxShft	DC.W 66,55,26,34,18,66,98,98
	EXPORT MaxAdj
MaxAdj	DC.W 32767,17407,16511,16414,16398,16446,32767
	EXPORT ZINS
ZINS	DC.B '0INN'
	EXPORT RndTbl
RndTbl	DC.B $00,$00,$00,$c0,$00,$c0,$00,$00,$00,$00,$00,$c0,$00,$c0,$00,$00,$00,$40,$00,$40,$00,$80,$00,$80
	ENDP
;          copy sane/fp816
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; File:  FP816
;; Description: Main routine for ORCA SANE 65816 floating point engine.
;; Status: Release 2.0
;;
;; Written by Kenton Hanson, Apple Numerics Group, 19 May 1986
;;
;; Modification History: 11Aug86 klh Halt vector mechanism fixed
;;	     26Feb87 klh Sets Invalid on integer overflow
;;
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
	EXPORT Eight16
Eight16	PROC
	jsl IncBusyFlg
	phd	; put direct reg on	stack
	tcd	; set direct reg
	pla	; save orig direct on zp
	sta OrigDirect
	phb	; save original data bank register
	phb
	pla
	sta OrigBank	; on zero page
	phk	; set data bank register
	plb	; this bank
	bra PassClayton
	EXPORT Clayton
Clayton		;       ;entry for Elems code
	jsl IncBusyFlg
	EXPORT PassClayton
PassClayton		; 
	pla
	sta ZPRtn
	pla
	sta ZPRtn+2
	pla
	sta ZPRtn+4
	stz ZeroWord	;initialize ZeroWord
	stz tmpFlgs	;initialize tmpFlgs
	stz Ctag	;initialize Ctag
	stz signC	;initialize signC
	pla
	sta opword	;save opword
	xba
	tay
	and #$7	;location of source	format
	asl a	;double it, for word based tables
	sta fence	;store source format (usually)
	tax	;also store in 'x'
	tya
	and #$70	;location of destination format
	beq z1s1
	lsr a
	lsr a
	lsr a
z1s1	sta fence2	;format of destination
	lda opword
	and #$1f	;mask out opcode
	sta opcode
	tay	;also store in 'y' for NonArith table2
	lsr a	;odd values are non	Arithmetic opcodes
	bcc z1s2	;Arithmetic function
	lda table2-1,y
	pha
	rts
z1s2	cmp #$7
	bcc z1s3	;a < 7, i.e., add, sub, mul, div, cmp, cpx, rem
	cmp #$0f	;NextAfter
	bne notASMD
	ldx fence
	stx fence2	;NextAfter, destination gets format of source
z1s3	stx fenceSv
	ldx fence2
	stx fence
	jsr UnPck	;get second argument (usually extended)
	moveA Brgstr,Crgstr
	jsr BtoA
	ldx fenceSv	;restore fence
	stx fence
;      ;ldx     fence
	ldy StkBtWd,x	;; tmpZP2 := max (lgthA, MaxShft,x)
	cpy lgthA	;StkBtWd,x - lgthC
	bpl z1s4
	ldy lgthA
z1s4	sty SBWord	;maximum length, i.e., max (lgthA, MaxShft,x)
	jsr UnPck	;get first argument
	lda signA
	eor signB
	sta signC
	lda fence2
	sta fence
	lda #NanTag
	cmp Atag	;NanTag - Atag
	bpl z1s5	;Nan found
	cmp Btag
	bmi z1s9	;neither value is a	Nan
	bpl z1s7	;only B is Nan
z1s5	cmp Btag	;NanTag - Btag
	bmi z1s6	;only A is a Nan
	lda FrctA
	cmp FrctB	;FrctA - FrctB
	bcc z1s7	;FrctA < FrctB
z1s6	jsr AtoC
	sec
	bcs z1s8	;branch always !!!!!!!!!
z1s11	jsr SetInv
z1s12	ldx #$0101	;return value for unordered
	stx pendingX
	jmp TrpHd1
z1s7	jsr BtoC
z1s8	lda opcode
	cmp #$8
	beq z1s12	;cmp
	cmp #$0a
	beq z1s11	;cpx
	jmp rtnArth
z1s9	lda rtnA
	pha
notASMD	ldy opcode
	lda table1,y
	pha
	rts
	EXPORT table1
table1	DC.W add-1,sub-1,mul-1,Xdiv-1,cmpar-1,cmpar-1,rem-1,z2x-1
	DC.W x2z-1,sqr-1,rndti-1,tti-1,scalb-1,logb-1,class-1,next-1
	EXPORT rtnA
rtnA		;       
	DC.W rtnArth-1
	EXPORT rtnB
rtnB		;       
	DC.W XitFP-1
	EXPORT table2
table2	DC.W setE-1,getE-1,setTV-1,getTV-1,D2B-1,B2D-1,neg-1,abs-1
	DC.W cpySgn-1,0,SetX-1,ProcIn-1,ProcX-1,testX-1
cmpar	pla	;unload rtnA, B rel	A
	lda rtnB
	pha
cmpar2	lda Atag
	bmi z2s2
	lda #-1
	bra z2s3	;branch always   !!!!!!
z2s2	tax
	inx
	bmi z2s3
	txa
z2s3	ldx signA
	bne z2s4
	eor #-1	;negate 'A'
	inc a
z2s4	sta tmpZP
	lda Btag
	bmi z2s5
	lda #-1
	bra z2s6	;branch always   !!!!!!
z2s5	tax
	inx
	bmi z2s6
	txa
z2s6	ldx signB
	bne z2s7
	eor #-1	;negate 'A'
	inc a
z2s7	cmp tmpZP	; B - A
	bmi z2s15	; B < A
	bne z2s17	; B > A
	lsr a
	bcc z2s13	; B = A
	sec
	lda expnB
	sbc expnA
	tay
	lda expnB+2
	sbc expnA+2
	bmi z2s14	; expnB < expnA
	bne z2s16	; expnB > expnA
	tya
	bne z2s16	; expnB > expnA
;	            ; expnB	= expnA
	ldx lgthB
	cpx lgthA	; lgthB - lgthA
	bpl z2s10
	ldx LgthA	; y := max (lgthB, lgthA)
z2s8	stz FrctB,X
	dex
	dex
	cpx lgthB	; x - lgthB
	bne z2s8
	ldx lgthA
	bpl z2s11
z2s9	stz FrctA,X
	dex
	dex
z2s10	cpx lgthA	; x - lgthA
	bne z2s9
	ldx lgthB
z2s11	stx tmpZP
	ldx #-2
z2s12	inx
	inx
	lda FrctB,x
	cmp FrctA,x	; FrctB - FrctA
	bcc z2s14	; FrctB < FrctA
	bne z2s16	; FrctB > FrctA
	cpx tmpZP	; x - tmpZP
	bmi z2s12
z2s13	ldx #$0002	; B = A
	bra endCmp
z2s14	lda signB
	bne z2s17	;both signs negative
z2s15	ldx #$8080	; B < A
	bra endCmp
z2s16	lda signB
	bne z2s15	;both signs negative
z2s17	ldx #$4040	; B > A
endCmp	stx pendingX
	rts
z2x	pop crgstr	;get second argument (usually extended)
	jsr UnPck	;get first argument
	lda fence2
	sta fence	;restore fence to ext. (usually) destination
	bra packit
x2z	pop crgstr	;get second argument
	lda fence	;save fence
	sta fenceSv
	lda fence2
	sta fence	;set fence to extended source
	jsr UnPck	;get first argument	(usually extended)
	lda fenceSv	;restore fence
	sta fence
packit	jsr BtoC
	jmp rtnArth
sqr	lda fence
	sta fence2
	jsr UnPck
	moveA Brgstr,Crgstr
	jsr sqrt
	jmp rtnArth
scalb	jsr UnPck
	moveA Brgstr,Argstr
	moveA Brgstr,Crgstr
	jsr BtoA
	clc
	pla	;get scale factor
	tax	;save sign of scale	integer
	adc expnB	;adjust exponent
	sta expnB
	txa
	bmi z3s7
	lda ZeroWord
	beq z3s8	;branch always !!!
z3s7	lda #-1
z3s8	adc expnB+2
	sta expnB+2
	bra packit
rndti	jsr UnPck
	moveA Brgstr,Crgstr
	jsr BtoC
	lda Ctag
	bmi z4s1
	jsr Rnd2Int
z4s1	jmp rtnArth
tti	jsr UnPck
	moveA Brgstr,Crgstr
	jsr BtoC
	lda Ctag
	bmi z5s1
	lda envWrd
	sta SBWord	;save rounding direction
	ora #$0c000	;set round towards zero
	sta envWrd
	jsr Rnd2Int
	lda SBWord
	and #$0c000
	sta SBWord	;get original rounding mode
	lda envWrd
	and #$03fff
	ora SBWord	;restore it
	sta envWrd
z5s1	jmp rtnArth
logb	lda fence
	sta fence2
	jsr UnPck
	moveA Brgstr,Crgstr
	lda rtnA
	pha
	lda Btag
	bmi z6s4
	lda #$2
	sta lgthC
	sec
	lda expnB
	sbc #$3fff
	sta FrctC+2
	lda expnB+2
	sbc zeroWord
	sta FrctC
	bpl z6s2
	sec
	lda ZeroWord
	sbc FrctC+2
	sta FrctC+2
	lda ZeroWord
	sbc FrctC
	sta FrctC
	inc signC
z6s2	stz expnC+2
	lda #$401e	;3fff + 31.
	sta expnC
	jsr NrmlzC
	lda Ctag
	bpl z6s3
	bit EnvWrd
	bpl z6s3	;either round to nearest or upward
	bvs z6s3	;round toward zero
	inc signC	;round downward with a zero result, rtn	-0
z6s3	rts
z6s4	inc a
	bne z6s6	;not zero
	lda #dvzXcp
	jsr setXcpn
	inc signC	;return negative infinity
z6s5	jmp rtnInf	;jsr, rts
z6s6	inc a
	beq z6s5	;infinity found
	jmp BtoC	;NaN found return input argument
class	jsr UnPck
	lda Btag
	and #$0ff	;clear high byte
	asl a
	lsr signB
	ror a	;put sign in high bit of word
	sta pendingX
	jmp XitFP
next	jsr cmpar2
	txa
	and #$0c000	;greater or less than
	bne BgNext
	jmp AtoC	;jsr, rts
BgNext	stz signB
	sta tmpZP
	bpl z7s1
	inc signB	;negate B
z7s1	eor signA
	sta signC	;initialize C for Add
	lda envWrd
	pha	;save rounding direction
	and #$3fff	;clear rounding direction
	ora tmpZP
	sta envWrd
	stz Btag	;put extremely small number in B
	stz expnB
	lda #$0f000
	sta expnB+2
	stz lgthB
	lda #$8000
	sta FrctB
	lda Atag
	cmp #InfTag
	bne z7s2
	stz Atag	;put extremely large number in A
	stz expnA
	lda #$0fff
	sta expnA+2
	stz lgthA
	lda #$8000
	sta FrctA
z7s2	jsr Add
	jsr ck4URO
;       ;;;;;;;;;;;;
	pla
	and #$0c000
	sta SBWord	;get original rounding mode
	lda envWrd
	and #$03fff
	ora SBWord	;restore it
	sta envWrd
	lda tmpFlgs
	and #$6	;underflow and overflow
	beq z7s5
	and #$2	;underflow
	bne z7s3	;underflow execption set
	lda Ctag
	bmi z7s7	;infinity delivered, save flags
	bpl z7s5
z7s3	lda FrctC
	bpl z7s7	;denormal number delivered, save flags
z7s5	stz tmpFlgs
z7s7	rts
	EXPORT rtnZero
rtnZero		;       
	lda #ZeroTag
	bne rtnInf2
	EXPORT rtnInf
rtnInf		;       
	lda #InfTag
rtnInf2	sta ctag
	lda #-2
	sta lgthC
	rts
	EXPORT InvNan
InvNan		;       
	stz lgthC
	EXPORT InvNan2
InvNan2		;       
	ora #$4000	;mask in 'non signaling bit'
	sta FrctC
	lda #NanTag
	sta Ctag
	EXPORT SetInvld
SetInvld		;       ;klh <26Feb87> new entry
SetInv	lda #invxcp
	EXPORT setXcpn
setXcpn		;       
	ora tmpFlgs
	sta tmpFlgs
	rts
SetE	pla
	sta EnvWrd
	jmp XitFP
GetE	ldx EnvWrd	;!!! Note entire EnvWrd goes to x !!!
	stx pendingX
	jmp XitFP
setTV	pop HaltVect
	jmp XitFP
getTV	ldx HaltVect
	stx pendingX
	ldy HaltVect+2
	sty pendingY
	jmp XitFP
neg	pop Brgstr
	ldy lastW,x	;get word with sign	bit
	cpx #$6
	bcs z8s3	;not an extended, double, or single
z8s2	lda [Brgstr],y
	eor #$8000	;change sign bit
	sta [Brgstr],y
	jmp XitFP
z8s3	cpx #$0c
	bcs z8s2	;not a long, integer, or comp type
NgIntgr	tyx
	ldy ZeroWord
	sec
z9s1	lda ZeroWord
	sbc [Brgstr],y
	sta [Brgstr],y
	iny
	dex
	iny
	dex
	bpl z9s1
	jmp XitFP
abs	pop Brgstr
	ldy lastW,x	;get word with sign	bit
	lda [Brgstr],y
	cpx #$6
	bcs z10s3	;extended, double, or single
z10s2	and #$7fff	;mask out sign bit
	sta [Brgstr],y
	jmp XitFP
z10s3	cpx #$0c
	bcs z10s2	;not a long, integer, or comp type
	asl a
	bcs NgIntgr
	jmp XitFP
cpySgn	pop Crgstr
	pop Brgstr
	ldy lastW,x	;get word (source) with sign bit
	lda [Brgstr],y
	ldx fence2	;get format of destination
	ldy lastW,x	;get word (destination) with sign bit
	cpx #$6
	bcs z11s3	;extended, double, or single (destination)
z11s2	asl a	;get sign bit of source into carry
	php	;save status (i.e.,	sign bit in carry)
	lda [Crgstr],y	;get sign word of destination
	asl a	;clear sign
	plp	;restore status (including sign bit in carry)
	ror a	;set sign bit of destination
	sta [Crgstr],y	;store updated sign	word (destination)
	jmp XitFP
z11s3	cpx #$0c
	bcs z11s2	;not a long, integer, or comp (destination)
	eor [Brgstr],y
	and #$8000
	bne NgIntgr	;signs different reverse sign of destination
	jmp XitFP
SetX	pla
	bne TH2
	jmp XitFP
TestX	pla
	xba
	and EnvWrd
	sta pendingX
	jmp XitFP
ProcIn	pop argstr
	ldy ZeroWord
	lda EnvWrd
	sta [argstr],y	;save current state
	stz EnvWrd	;set default state
	jmp XitFP
ProcX	lda EnvWrd	;get current exceptions
	xba	;swap bytes
	sta tmpflgs	;let trap handler mask in exceptions
	pla	;get old environment
	sta EnvWrd
	jmp TrpHd1
rtnArth	jsr pck
	EXPORT TrpHd1
TrpHd1		;       
	lda tmpflgs
TH2	and #$1F
	bit EnvWrd
	beq z12s7
	ldy HaltVect
	bne HaltMech
	ldy HaltVect+2
	beq z12s7
	bra HaltMech

setHV	ldy HaltVect+1	; get 3-byte Halt Vector address - 1
	lda HaltVect	; onto stack, then RTL
	bne noCryHV	; dec HaltVector does not change hi word
	dey
noCryHv	dec a
	phy	; push 2 hi order bytes
	phb	;dummy push opf DBR	to decrement S by 1
	sta 1,s	; store 2 low order	bytes on stack
	rtl	; note jsr (below) put correct return
*		; on stack

HaltMech	jsl setHV	; store return address for
*		; effective jsl to HaltVect-1
	lda tmpflgs	; <klh 8Aug86>
	and #$1F	; <klh 8Aug86>
z12s7	xba
	ora EnvWrd
	sta EnvWrd
	EXPORT XitFP
XitFP		;       
	bit pendingX
	lda pendingX
	ldy pendingX+1
	tax
	and #$ff00
	pei ZPRtn+4
	pei ZPRtn+2
	pei ZPRtn
	php
	lda OrigBank	; restore original data bank
	pha
	plb
	plb
	lda OrigDirect	; restore orig direct
	tcd
	lda ZeroWord	; <klh 8Aug86>
	plp
	clc
	jsl DecBusyFlg
	rtl
;-
;+
	RtoR A,C
	RtoR B,A
	RtoR B,C
	ENDP
;          copy sane/unpack
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; File:  Unpack
;; Description: Unpack and Pack for ORCA SANE 65816 floating point engine
;; Status: Release 2.0
;;
;; Written by Kenton Hanson, Apple Numerics Group, 21 May 1986
;;
;; Modification History:  
;;
;; 26Feb87 	klh 
;;
;; Sets Invalid on integer overflow
;;
;; 12 Dec 88	Kenton Hanson
;;
;; Fixed bug of conversion of large comps to extended.
;; Floating point nan conversion to comp nan no longer signals invalid.
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
	EXPORT UnPck
UnPck	PROC
	plx	;save return from UnPck
	pop Brgstr
	phx	;restore return from UnPck
	stz btag	;initialize tag for	normal nonzero number
	stz signB	;initialize sign to	'+'
	stz expnB+2	;initialize high word of exponent to zero
	ldx fence
	ldy lastW,x
	cpx #ffLng
	bcc fpUnPck	; y < ffLng, i.e., Extended Double or Single
	cpx #ffq
	bcs fpUnPck	; y >= ffq
;fxdUnPck
	stz FrctB+2
	stz FrctB+4
	stz FrctB+6
	stz FrctB+8	; added by Kenton 12 Dec 88
	lda [BRgstr],y
	bpl PosFxd
	tyx
	ldy signB	;ldy    #00
	inc signB	;set signB to 1 => '-'
	sec
z1s1	lda btag	;lda    #00
	sbc [BRgstr],y
	sta FrctB,x
	iny
	dex
	iny
	dex
	bpl z1s1
	tax
	bpl NrmlWrd
	lda fence
	cmp #ffComp
	bne NrmlWrd	;not a comp NaN
	stz lgthB
	lda #NaNComp
	sta FrctB
	lda #NanTag
	sta btag
	rts
PosFxd	tyx
	ldy signB	;ldy    #00
z2s1	lda [BRgstr],y
	sta FrctB,x
	iny
	dex
	iny
	dex
	bpl z2s1
NrmlWrd	lda FrctB
	bne z3s1
	lda FrctB+2
	sta FrctB
	lda FrctB+4
	sta FrctB+2
	lda FrctB+6
	sta FrctB+4
	stz FrctB+6	;clear last word
	dey
	dey
	bne NrmlWrd
	stz lgthB	;make sure length is not total garbage
	jmp ZrTagB
z3s1	tya
	sta lgthB
	asl a
	asl a
	asl a
	adc #$3ffe	;add correct bias
	sta expnB
	jmp normB
fpUnPck	lda [BRgstr],y
	asl a
	rol signB	;set sign word 0 for '+', 1 for '-'
	lsr a	;strip off sign bit
	sta expnB	;store low word of exponent
	ldx #-2
	stx lgthB	;initialize length of fraction to -2
	dey
	dey
z4s1	inx
	inx
	lda [BRgstr],y
	beq z4s2	;zero word found don't update length
	stx lgthB	;update length
z4s2	sta FrctB,x
	dey
	dey
	bpl z4s1
	ldy fence
	beq z4s9	;unpacking an extended
	cpy #ffq
	bcs z4s9	; y >= ffq
	ldx lastW,y	;either a double or	a single
	stx lgthB	;set max length
	stz FrctB,X	;zero last word
	ldx ShftDS-2,y	;get # of left shifts
z4s3	lsr expnB	;right shift x bits
	ror FrctB
	ror FrctB+2
	ror FrctB+4
	ror FrctB+6
	dex
	bne z4s3
	lda expnB
	sec	;set implicit bit
	bne z4s4	;normalized number found
	inc btag	;set tag to 1
	inc expnB	;increment exponent
	clc	;clear implicit bit
z4s4	ror FrctB
	ror FrctB+2
	ror FrctB+4
	ror FrctB+6
	jsr TailOff
	lda expnB
	cmp MaxExp,y
	bcs NanInf	; a >= MaxExp,y
	adc ExpAdj-2,y
	sta expnB
z4s9	lda expnB
	bne z4s10
	ldx FrctB
	bmi z4s10	;not a denormalized	extended
	inc btag	;denormalized number found
z4s10	cmp #$7fff
	beq NanInf	;maximum exponent (either a NaN or Infinity)
	ldx lgthB
	bpl normB	;not a zero
ZrTagB	lda #ZeroTag	;zero found, set tag (=-1), length to -2 & exit
	sta Btag
	dec a
	sta lgthB
	rts
;       ; handle NaNs and Infinities
NaNInf	lda FrctB
	ldx #-2	;infinity tag
	ldy lgthB
	bmi z5s2	;infinity found
	bne z5s1
	cmp #$8000	;infinity, if this is the only bit set!
	beq z5s2	;only the bit left of decimal is set, i.e. inf.
z5s1	dex	;set x to (-3) quiet NaN
	ora #$4000	;mask in non signaling NaN bit
	cmp FrctB
	beq z5s2	;quiet NaN
	dex	;signalling NaN (-4)
	sta FrctB	;change to quiet NaN
	lda tmpflgs	;set invalid exception
	ora #invxcp
	sta tmpFlgs
z5s2	stx btag
	jmp TailOff
	EXPORT BgnNrmB
BgnNrmB		;       
	ldx lgthB	;left shift one bit
	clc
z6s1	rol FrctB,x
	dex
	dex
	bpl z6s1
	lda expnB	;increment exponent
	bne z6s2
	dec expnB+2
z6s2	dec expnB
normB	lda FrctB
	bpl BgnNrmB	;still not normalized
TailOff	ldx lgthB
	bmi z7s2
z7s1	lda FrctB,x	;strip trailing zero words
	bne z7s2
	dex
	dex
	stx lgthB
	bpl z7s1
z7s2	rts	;END OF UNPACK   END OF UNPACK   END OF	UNPACK
	EXPORT pck
pck		;       
	ldx fence
	ldy ctag
	bmi ZIN	;either a zero, infinity or NaN
	jsr ck4URO	;check for underflow, round, check for overflow
StuffIt	ldx fence
;      ;beq     StuffEx         ;extended
	bne z8s1
	jmp StuffEx
z8s1	cpx #ffLng
	bcc PkDblSg	; x < ffLng, i.e., Double or Single
	cpx #ffq
	bcc @skip	; Changed 12 Dec 88 to handle long branch
	brl StuffEx	; x >= ffq
@skip
;   ;integer longInt or comp
	ldy lastW,x
	ldx signC	; dual purpose, also initializes x to zero
	beq z8s3	;LgInCmp         ; branch to long, integer, Comp
	tyx
	sec
z8s2	lda ZeroWord
	sbc FrctC,x
	sta FrctC,x
	dex
	dex
	bpl z8s2
	ldx ZeroWord
z8s3	
	bne ZIN	; changed 12 Dec 88 to handle long branch
	brl LgInCmp	; branch to long, integer, Comp

ZIN	iny
	bne LdMxXp	;Infinity or NaN Encountered
	stz lgthC	;zero Encountered
	lda lgthC
	dec lgthC	;make sure lgthC is	negative
	cpx #ffLng
	bcc stffItZ	; x < ffLng, i.e., Extended, Double or Single
	cpx #ffq
	bcc ZeroILC	; force positive zero for Long, integer	& comp
	bcs StffItZ	; x >= ffq
LdMxXp	lda MaxAdj,x
	sta expnC
	cpx #ffLng
	bcc stuffIt	; x < ffLng, i.e., Extended, Double or Single
	cpx #ffq
	bcs StuffIt	; x >= ffq

	cpx #ffComp	; added 12 Dec 88
	bne seti88
	iny
	bmi nSet88	; end addition

seti88	jsr SetInvld	; 26Feb87 klh Sets Invalid on integer overflow
nSet88	ldy lastW,x
	stz lgthC
	ldx lgthC
	lda #$8000
	bne LgInCmp2	; branch to long, integer, Comp
PkDblSg	ldy ShftDS-2,x	;number of left shifts to align dbl & sgl
	ldx lgthC
	bmi z9s2
	clc
z9s1	rol FrctC,x	;shift one bit to set carry
	dex
	dex
	bpl z9s1
z9s2	ldx Ctag
	inx
	bne z9s3	;not zero
	txa	;ldaI   0
	beq stffitz
z9s3	bpl z9s4	;not infinity or a Nan
	sec	;need to set carry for subtract below
z9s4	ldx fence
	lda expnC
	sbc ExpAdj-2,x	;carry set depending on implicit bit above (rol)
	sta expnC
z9s5	clc
	ldx lgthC
	bmi z9s7
z9s6	rol FrctC,x	;finish shifting
	dex
	dex
	bpl z9s6
z9s7	rol expnC
	dey
	bne z9s5
StuffEx	lda expnC
	asl a
stffitz	lsr signC	;shift sign bit into carry
	ror a
ZeroILC	ldx fence
	ldy lastW,x
	sta [CRgstr],y	;store sign & exponent
	ldx #-1
	dey
	bmi IntFrmt
loop8	cpx lgthC
	bpl FillZWd	; x >= lgthC
	inx
	dey
LgInCmp	lda FrctC,x
LgInCmp2	sta [CRgstr],y
	inx
	dey
	bpl loop8
IntFrmt	rts
FillZWd	lda #$0	;fill out zero words
z10s10	dey
	sta [CRgstr],y
	dey
	bpl z10s10
	rts
	EXPORT ck4URO
ck4URO		;       
	ldx fence
	lda #$00c0	;mask for Rounding Precision
	and EnvWrd
	beq noRP
	lsr a
	lsr a
	lsr a
	lsr a
	lsr a
	cmp fence	;a (RP) - fence
	bcc noRP	;a < fence
	tax
noRP	stx fenceSV	;local fence
	cpx #ffqq
	beq RoundIt	;no underflow checking
	clc
	lda MinExp,x
	sbc expnC	;MinExp,x - expnC -	1
	sta expnX	;save low word of subtract
	lda ZeroWord
	sbc expnC+2
	bmi RoundIt	;no underflow, go to rounding code
	sta expnX+2
	inc expnX
	bne z11s1
	inc expnX+2
z11s1	lda MinExp,x	;store minimum exponent
	sta expnC
	stz expnC+2
	jsr DnrmC
RoundIt	ldy fenceSV
	ldx lastSig,y
	stx TmpZP4	;save lastSig,y for	later update of lgthC
	dey
	bmi z12s5	;extended, bypass this double & single stuff
	dey
	bne z12s2
	lda #$4
	cmp lgthC
	bpl NoRnd	;4 >= lgthC
	lda FrctC+6
	and #$800
	sta TmpZp2	;store lsb in TmpZP2
	lda FrctC+6
	and #$400
	sta TmpZP	;store round bit in	TmpZP
	lda FrctC+6
	sta TmpZP3
	and #$0f800
	sta FrctC+6	;clear bits after lsb
	lda #$3ff
	bne z12s4
z12s2	dey
	dey
	bne z12s5
	lda lgthC
	beq NoRnd
	lda FrctC+2
	and #$100
	sta TmpZp2	;store lsb in TmpZP2
	lda FrctC+2
	and #$80
	sta TmpZP	;store round bit in	TmpZP
	lda FrctC+2
	sta TmpZP3
	and #$0ff00
	sta FrctC+2	;clear bits after lsb
	lda #$7f
z12s4	and TmpZP3	;get sticky bits into A
	clc	;branch always   branch always   branch	always
	bcc StkBtLp	;branch always   branch always   branch	always
z12s5	cpx lgthC	;x - lgthC
	bmi Rnding	;lastSig,y < lgthC
NoRnd	jmp OvrflCk
Rnding	lda FrctC,x
	and #$1
	sta tmpZP2	;store lsb in tmpZP2
	stz tmpZP
	inx
	inx
	lda FrctC,x
	asl a	;store sticky bit in A
	ror tmpZP	;store round bit in	tmpZP
StkBtLp	tay	;Sticky Bit Loop, put sticky bits in 'A'
	bne z13s7	;sticky bit found
z13s6	cpx lgthC	;x - lgthC
	bpl z13s7	;y >= lgthC, no more words to examine
	inx
	inx
	ora FrctC,x	;or sticky bits
	beq z13s6	;no sticky bit, continue looking
z13s7	sta tmpZP3	;save sticky bits
	ldx TmpZp4	;get lastSig,y(fenceSV)
	stx lgthC	;update length of C
	ora tmpZP	;or sticky bit with	round bit
	beq DumpTrZ	;no trailing bits, therefore, no rounding
	lda tmpflgs	;set inexact exception
	ora #inexcp
	ldx FrctC
	bmi z13s8	;normalized number,	do not set underflow
	ldy fenceSV
	cpy #ffLng
	bcc UnderF	;extended, double, or single
	cpy #ffq
	bcc z13s8	;long, integer, or Comp
UnderF	ora #unfxcp
z13s8	sta tmpFlgs
	lda #$0c000	;mask in rounding direction
	bit EnvWrd
	beq RndNr
	bmi z13s9	;either downward or	toward zero
	lda signC	;round upward
	beq RndUp	;sign is positive
	bne DumpTrZ	;no rounding up (in	magnitude)
z13s9	bvs DumpTrZ	;toward zero
	lda signC	;round downward
	bne RndUp	;sign is negative
	beq DumpTrZ	;no rounding up (in	magnitude)
RndNr	lda tmpZp	;get round bit
	beq DumpTrZ	;no round bit, dump	trailing zero words
	lda tmpZP3	;get sticky bits
	ora tmpZP2	;or lsb & sticky bit
	beq DumpTrZ	;no rounding up (in	magnitude)
;	    environment word is rr-xdoui RR-XDOUI
;		    rd xcpns rp halts
;               0 to nearest, 1 upward,	2 downward, 3 toward zero
RndUp	ldy fenceSV
	ldx LastSig,y
	dey
	bmi z14s3	;extended format, bypass double & single check
	dey
	bne z14s1	;not a double
	lda #$800
	bne z14s2	;branch always
z14s1	dey
	dey
	bne z14s3	;not a single
	lda #$100
z14s2	clc
	adc FrctC,x
	sta FrctC,x
	bcc DumpTrZ
	bcs z14s4
z14s3	inc FrctC,x
	bne DumpTrZ
z14s4	dex
	dex
	bpl z14s3
	sec
	ror FrctC	;right shift signicand
	inc expnC	;adjust the exponent
	bne DumpTrZ
	inc expnC+2
DumpTrZ	ldx lgthC	;dump trailing Zero	words
	lda FrctC,x
	bne OvrflCk	;non zero word found, exit to Overflow check
	dec lgthC
	dec lgthC
	bpl DumpTrZ
	ldy fence
	lda MinExp,y
	sta expnC
	jmp rtnZero
OvrflCk	ldy fenceSV
	cpy #ffLng
	bcc OvfEDS	; x < ffLng, i.e., Extended, Double or Single
	cpy #ffq
	bcs OvfR	; x >= ffq
	ldx FrctC
	bpl RtsOut	;no integer overflow, leading bit is off
	lda signC
	beq MxNgI	;leading bit on, but number is positive
	lda expnC+2
	bne MxNgI	;overflow
	lda expnC
	cmp MaxAdj,y
	bne z15s1	;expnC <> MaxAdj,y
	lda lgthC
	bne MxNgI	;overflow, length greater than one word
	cpx #$8000
	beq RtsOut	;max negative number
	bne MxNgI
z15s1	bcs MxNgI	;expnC > MaxAdj,y
RtsOut	rts
MxNgI	lda #$8000	;Stuff Maximum Negative Integer
	sta frctC
	stz lgthC
	jsr SetInvld	; 26Feb87 klh Sets Invalid on integer overflow
	jmp rtnInf
OvfR	cpy #$0e
	beq RtsOut
OvfEDS	lda expnC+2
	bmi RtsOut
	bne Ovrfld
	lda expnC
	cmp MaxAdj,y	;expnC - MaxAdj,y
	bcc RtsOut	;expnC < MaxAdj,y
;Ovrfld  ;set inexact and overflow
Ovrfld	lda tmpflgs
	ora #ovfxcp+inexcp
	sta tmpflgs
	stz expnC+2	;stuff maximum exponent
	lda MaxAdj,y
	sta expnC
	lda #$0c000	;mask for rounding direction bits
	bit EnvWrd
	beq OvrInf	;Round to nearest, set infinity
	bvc RNlbl	;Round to negative
	bmi MaxVal	;Round toward zero,	deliver maximum value
	lda signC	;Round postive
	bne MaxVal	;RP and negative value
;	            ;RP and	positive value
OvrInf	ldy fence
	lda MaxAdj,y
	sta expnC
	jmp rtnInf	;return infinity
RNlbl	lda signC
	bne OvrInf	;RN and negative value
;	            ;RN and	positive value
MaxVal	dec expnC	;decrement exponent	to maximum numeric value
	ldx lastSig,y
	stx lgthC
	lda #-1	; FFFF
z16s1	sta FrctC,x
	dex
	dex
	bpl z16s1
	dey
	dey
	bne z16s2	;not a double
	lda #$0f800
	sta FrctC+6
z16s2	dey
	dey
	bne RtsOut	;not a single
	lda #$0ff00
	sta FrctC+2
	rts
;-
;+
	ENDP
;          copy sane/asmdrs
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; File:  ASMDRS
;; Description: Add	Subtract Multiply, Divide, Remainder
;;              and	Squareroot, for ORCA SANE 65816 floating point engine
;; Status: Alpha
;;
;; Written by Kenton Hanson, Apple Numerics Group, 20 May 1986
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
	EXPORT Sub
Sub	PROC
	lda #$1
	eor signB	;reverse sign of source
	sta signB
	eor signA
	sta signC
	EXPORT Add
Add		;       
	ldx Atag	;A - B
	bmi z1s3
	ldy Btag
	bpl BgnAdSb	;no special cases
	iny
	beq z1s6	;B is zero and A is	a number, return A
	bmi z1s4	;B is infinity and A is a number, return B
z1s3	ldy Btag
	inx
	bmi z1s5	;A is infinity
	iny	;A is zero
	beq z1s9	;A is zero and B is	zero, return a zero
;	            ;A is zero and B is not a zero, return B
z1s4	jmp BtoC	;return B
z1s5	iny
	bmi z1s7	;A is infinity and B is infinity, check	signs
;	            ;A is infinity and B is not	infinity, return A
z1s6	jmp AtoC	;return A
z1s7	lda signC
	beq z1s6	;signs of infinities are compatible
	lda #NanAdd
	jmp InvNan
z1s9	lda signC
	beq z1s6	;sum of zeros with same sign
	jmp ZrRslt	;0-0 or 0+(-0), ck rounding direction for sign
BgnAdSb	sec
	lda expnA
	sbc expnB
	sta expnX
	lda expnA+2
	sbc expnB+2
	sta expnX+2
	bmi AxltBx	;expnA < expnB
;	            ;expnA >= expnB
	RtoRXpn B,C,A	;B to C with sign &	exponent of A
	jmp DMC	;Branch always !!!!!!!!!!!!!!!!!!
	EXPORT AxltBx
AxltBx		; 
	RtoRXpn A,C,B	;A to C with sign &	exponent of B
	lda signA
	sta signB
	ldx lgthB
	stx lgthA
z2s1	lda FrctB,x
	sta FrctA,x
	dex
	dex
	bpl z2s1
	ldx fence
	ldy StkBtWd,x	;; SBWord := max (lgthA, MaxShft,x)
	cpy lgthA	;StkBtWd,x - lgthA
	bpl z2s3
	ldy lgthA
z2s3	sty SBWord	;maximum length, i.e., max (lgthA, MaxShft,x)
	sec	;expnX := -expnX
	lda ZeroWord
	sbc expnX
	sta expnX
	lda ZeroWord
	sbc expnX+2
	sta expnX+2
DMC	jsr dnrmC
	ldx lgthC
z3s1	cpx lgthA	;lgthC - lgthA
	bpl z3s2
	inx
	inx
	stx lgthC	;extend length of C	to A
	stz FrctC,X
	bpl z3s1	;branch always  !!!!!!!!!!!!!!!!
z3s2	lda signB
	eor signC
	bne diffSgn
	ldx lgthA
	clc
z3s5	lda FrctA,x
	adc FrctC,x
	sta FrctC,x
	dex
	dex
	bpl z3s5
	bcc z3s10	;no carry overflow
	ldx lgthC
	txy
	ldx ZeroWord
z3s6	ror FrctC,x
	inx
	dey
	inx
	dey
	bpl z3s6
	inc expnC	;adjust exponent
	bne z3s7
	inc expnC+2
z3s7	bcc z3s10	;no carry from right bit shift
	ldx lgthC
	cpx SBWord	;lgthC - SBWord
	bcc z3s8	;lgthC < SBWord
	lda FrctC,x
	ora #$1	;or carry out, into	last bit of FrctC,x
	bne z3s9	;always true
z3s8	inx	;add new word with a leading bit set
	inx
	stx lgthC
	inc tmpZP2	;update word length
	lda #$8000
z3s9	sta FrctC,x
z3s10	rts
diffSgn	ldx lgthA
z4s1	cpx lgthC	;lgthA - lgthC
	bpl z4s2
	inx
	inx
	stx lgthA	;extend length of A	to C
	stz FrctA,X
	bpl z4s1	;branch always  !!!!!!!!!!!!!!!!
z4s2	sec
z4s5	lda FrctA,x
	sbc FrctC,x
	sta FrctC,x
	dex
	dex
	bpl z4s5
	bcs NrmlzC	;|A| >= |C|
	ldx lgthC	;negate result
	sec
z4s6	lda ZeroWord
	sbc FrctC,x
	sta FrctC,x
	dex
	dex
	bpl z4s6
	lda signB	;change sign
	sta signC
	EXPORT NrmlzC
NrmlzC		;       
	lda FrctC
	bne NrmBitC	;leading word non zero
	ldx #-2
	lda lgthC
	beq ZrRslt	;zero result
	lsr a	;number of word shifts
z5s1	inx
	inx
	ldy FrctC+2,x
	sty FrctC,x
	dec a
	bne z5s1
	stx lgthC	;store updated length of C
	lda expnC	;Note: carry cleared by 'lsr' above
	sbc #$0f	;really expnC := expnC - 16.
	sta expnC
	bcs NrmlzC
	dec expnC+2
	bcc NrmlzC	;branch always
ZrRslt	stz signC
	lda EnvWrd
	and #$0c000	;mask in rounding direction
	asl a
	bcc z6s1
	bne z6s1	;RZ
	rol signC
z6s1	jmp rtnZero
loopNBC	ldx lgthC
	clc
z7s3	rol FrctC,x
	dex
	dex
	bpl z7s3
	lda expnC
	bne z7s4
	dec expnC+2
z7s4	dec expnC
	lda FrctC
NrmBitC	bpl loopNBC
	rts
;	            ;A div B
	EXPORT Xdiv
Xdiv		;       
	ldx Atag
	bmi z8s5
	ldy Btag
	bpl BgnDiv	;no special cases
	iny
	bmi z8s6	;B is infinity and A is a number, return zero
;	            ;B is zero and A is a number, return infinity
	lda #dvzxcp
	jsr setXcpn	;signal div_zero
z8s4	jmp rtnInf
z8s5	cpx Btag
	beq z8s7	;0/0 or infinity/infinity
	inx
	bmi z8s4	;A is infinity and B is not, return infinity
;	            ;A is zero and B is not zero, return zero
z8s6	jmp rtnZero
z8s7	lda #NanDiv
	jmp InvNan
BgnDiv	jsr rtab1X	;right shift A and B one bit
;	            ;& extend the length of A to B if necessary
	ldx fence2	;format of destination
	lda MaxShft,x
	sta tmpZP2
	lsr tmpZP2	;divide by 16, to find the # of word divides
	lsr tmpZP2
	lsr tmpZP2
	lsr tmpZP2	;number of full word divides
	and #$0f
	sta tmpZP3	;number of bit divides for last word
	stz lgthC	;initialize length of 'C'
z9s4	jsr dvword	;divide a word
	ldx lgthC
	lda tmpZP
	sta FrctC,x	;store byte of quotient
	inc lgthC
	inc lgthC
	dec tmpZP2
	bne z9s4
	ldy tmpZP3
	jsr dvword2
	lda tmpZP
	lsr a
z9s5	ror a	;adjust last word of quotient
	dec tmpZP3
	bne z9s5
	ldx lgthC
	sta FrctC,x	;store word of quotient
	ldx lgthA
z9s7	lda FrctA,x	;check for sticky bit loop
	bne z9s8	;found a sticky bit
	dex
	dex
	bpl z9s7
	bmi z9s9	;branch always, no sticky bit
z9s8	ldx lgthC
	lda #$1
	ora FrctC,x	;or in sticky bit
	sta FrctC,x	;store sticky bit
z9s9	sec
	lda expnA
	sbc expnB
	sta expnC
	lda expnA+2
	sbc expnB+2
	sta expnC+2
	lda FrctC
	asl a
	bcs z9s13	;leading bit set, no left shift necessary
	ldx lgthC
	clc
z9s12	rol FrctC,x
	dex
	dex
	bpl z9s12
z9s13	lda expnC	;adjust exponent
	adc #$3ffe
	sta expnC
	lda expnC+2
	adc ZeroWord
	sta expnC+2
z9s15	ldx lgthC	;strip trailing zero word loop
	lda FrctC,x
	bne z9s20	;word contains a bit, exit loop
	dec lgthC
	dec lgthC
	bpl z9s15
z9s20	rts
dvword	ldy #$10	;set bit counter
dvword2	stz tmpZP	;initialize quotient word
dvword3	lda FrctA
	cmp FrctB	;compares MSB of FrctA - FrctB
	bcc z10s4	;early bypass, FrctA < FrctB
	ldx lgthB	;carry is set
z10s2	lda FrctA,x
	sbc FrctB,x	;FrctA := FrctA - FrctB loop
	sta FrctA,x
	dex
	dex
	bpl z10s2
	bcs z10s4	;FrctA - FrctB >= 0
	ldx lgthB	;carry is clear
z10s3	lda FrctA,x
	adc FrctB,x	;restore loop
	sta FrctA,x
	dex
	dex
	bpl z10s3
	clc
z10s4	rol tmpZP
	ldx lgthA
	clc
z10s5	rol FrctA,x	;left shift 'A'
	dex
	dex
	bpl z10s5
	dey	;decremet bit counter
	bne dvWord3
	rts
rtab1X	ldx ZeroWord	;shift anm "1" bit to right ********
	lda lgthA
	lsr a	;divide by 2, i.e. number of words in 'A'
;      ;clc	            ;Note:	above instruction clears carry
z11s1	ror FrctA,x	;right shift byte
	inx
	inx
	dec a
	bpl z11s1
	bcc rtb1	;no carry bit
	stx lgthA	;store incremented anm
	stz FrctA,X
	ror FrctA,x	;shift in carry
rtb1	ldx ZeroWord	;shift anm "1" bit to right ********
	lda lgthB
	lsr a	;divide by 2, i.e. number of words in 'B'
;      ;clc	            ;Note:	above instruction clears carry
z12s1	ror FrctB,x	;right shift byte
	inx
	inx
	dec a
	bpl z12s1
	bcc XAtoB	;no carry bit
	stx lgthB	;store incremented anm
	stz FrctB,X
	ror FrctB,x	;shift in carry
XAtoB	ldx lgthA
z13s1	cpx lgthB	;lgthA - lgthB
	bcs z13s2	;lgthA >= lgthB
	inx
	inx
	stx lgthA	;store new length
	stz FrctA,X	;pad FrctA with zeros
	bpl z13s1	;always true
z13s2	rts
	EXPORT rem
rem		;       
	lda signC
	lsr a
	stz pendingX	;put sign of C into	pendingX
	ror pendingX	;in the high bit of	both bytes
	xba
	ora pendingX
	sta pendingX
	lda signA
	sta signC	;initialize sign
	lda atag
	ldx btag
	bmi z14s1
	tay
	bpl BgnRem	;no special cases
z14s1	cmp #inftag
	beq z14s2	;g,h,i          infinity rem [ ]
	cpx #ZeroTag
	beq z14s2	;a,d            [ ]	rem 0
	jmp AtoC	;rts     b,c,f	    0 rem [w, infinity]
;		    ;or w rem infinity
z14s2	lda #NaNRem
	jmp invNaN	;rts
BgnRem	jsr rtab1X	;right shift A and B one bit
;	            ;& extend the length of A to B if necessary
	stz tmpZP
	sec
	lda expna
	sbc expnb
	sta expnc
	lda expna+2
	sbc expnb+2
	sta expnc+2
	inc expnC	;add 1
	bne z15s2
	inc expnC+2
z15s2	bit expnC+2
	bmi z15s13
	ldy #$1
	jsr dvword2
	ldy expnC
	beq z15s3
	jsr dvword3
z15s3	dec expnc+2	;sub 1
	bmi z15s5
	ldy ZeroWord
	jsr dvword3	;i.e. do 2^16 cycles
	bra z15s3	;always true
z15s5	clc	;subtract 1
	lda ExpnB
	sbc ZeroWord
	sta ExpnC
	lda ExpnB+2
	sbc ZeroWord
	sta ExpnC+2
	lsr tmpZP
	lda tmpZP
	sta tmpZP4
	bcc z15s23	;a < b/2
	ldx lgthA
z15s7	lda FrctA,x
	bne z15s8	;non zero anm
	dex
	dex
	bpl z15s7
	lsr tmpZP
	bcc z15s9	;Q is even
z15s8	lda tmpZP4	;carry is set
	adc ZeroWord	;clears carry, for $9 below
	sta tmpZP4
	lda signC
	eor #$1	;change sign
	sta signC
z15s9	ldx lgthB
;      ;clc
z15s10	rol FrctB,x
	dex
	dex
	bpl z15s10
	ldx lgthB
z15s22	cpx lgthA
	bcs z15s11	; x >= lgthA
	inx
	inx
	stz FrctB,X
	bpl z15s22	; always true
z15s11	lda FrctB,x
	sbc FrctA,x	;a := b - a
	sta FrctA,x
	dex
	dex
	bpl z15s11
z15s23	lda tmpZP4
	and #$7f
	ora pendingX
	sta pendingX
	ldx lgthA
z15s12	lda FrctA,x
	bne z15s15
	dex
	dex
	bpl z15s12
	jmp rtnZero	;rts
z15s13	sec	;add 1
	lda expnA
	adc ZeroWord
	sta expnC
	lda expnA+2
	adc ZeroWord
	sta expnC+2
z15s15	ldx lgthA
	stx lgthC
z15s16	lda FrctA,x
	sta FrctC,x
	dex
	dex
	bpl z15s16
	tax
	bmi EndRem
	EXPORT BgnNrmC
BgnNrmC		;       
	ldx lgthC	;left shift one bit
	clc
z16s1	rol FrctC,x
	dex
	dex
	bpl z16s1
	lda expnC	;increment exponent
	bne z16s2
	dec expnC+2
z16s2	dec expnC
normC	lda FrctC
	bpl BgnNrmC	;still not normalized
TailOff	ldx lgthC
	bmi EndRem
z17s1	lda FrctC,x	;strip trailing zero words
	bne EndRem
	dex
	dex
	stx lgthC
	bpl z17s1
EndRem	rts
	EXPORT sqrt
sqrt		;       
	lda signB
	sta signC
	beq z18s3	;positive argument
	lda Btag
	bpl z18s1	;sqrt of non zero negative number
	cmp #Inftag
	bne z18s2	;either '0' or a Nan
z18s1	lda #NanSqrt
	jmp InvNaN	;rts
z18s2	jmp BtoC	;rts
z18s3	lda Btag
	bmi z18s2
	jsr rtB1
	clc
	lda #$4001	;(3fff + 2)
	adc expnB	;adjust exponent and transfer to expnC
	sta expnC
	lda ZeroWord
	adc expnB+2
	sta expnC+2
	lda expnC+2
	asl a	;extend sign
	ror expnC+2
	ror expnc
	bcs z18s5
	jsr rtB1
z18s5	ldx fence
	ldy MaxShft,x
	lda #$2000
	stz lgthC	;initialize lgthC
	ldx lgthC
	sta FrctC
	sta tmpZP	;initialize tmpZP
	beq z18s12	;always true
z18s8	lsr tmpZP	;right shift mask
	bcc z18s10	;mask ok
	lda #$8000
	sta tmpZP	;reset mask
	inc lgthC	;expand lgthC
	inc lgthC
	ldx lgthC
	stz FrctC,X
	ldx lgthB
	cpx lgthC
	bcs z18s9	;lgthB ok
	inx
	inx
	stx lgthB	;expand lgthB
	stz FrctB,X
z18s9	clc
z18s10	ldx lgthB
z18s11	rol FrctB,x	;left shift FrctB 1	bit
	dex
	dex
	bpl z18s11
	ldx lgthC
	lda FrctC,x
	ora tmpZP
	sta FrctC,x	;or mask with FrctC
z18s12	lda FrctB
	cmp FrctC
	bcc z18s16	;early bypass
	ldx lgthC
z18s13	lda FrctB,x	;subtact loop
	sbc FrctC,x
	sta FrctB,x
	dex
	dex
	bpl z18s13
	ldx lgthC
	bcc z18s15	;lgthB < lgthC
	clc
	lda tmpZP
z18s14	adc FrctC,x	;add mask i.e. really 2*mask
	sta FrctC,x
	bcc z18s17
	lda ZeroWord
	dex
	dex
	bpl z18s14
z18s15	lda FrctB,x
	adc FrctC,x	;restore FrctB loop
	sta FrctB,x
	dex
	dex
	bpl z18s15
z18s16	ldx lgthC
	lda tmpZP
	eor FrctC,x	;restore FrctC
	sta FrctC,x
z18s17	dey
	bpl z18s8	;need another bit
	ldx lgthB
z18s18	lda FrctB,x
	bne z18s19	;sticky bit found
	dex
	dex
	bpl z18s18
	bmi z18s20	;always true, no sticky bit found
z18s19	ldx lgthC
	lda tmpZP
	ora FrctC,x	;insert sticky bit
	sta FrctC,x
z18s20	jmp BgnNrmC	;rts
	ENDP
;          copy sane/b2d2b
;he     ''B2D2B'17 April 86
;fo     ''%'klh
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; File:  B2D2B
;; Description: Binary <--> Decimal for	ORCA SANE 65816 floating point engine
;; Status: Alpha
;;
;; Written by Kenton Hanson, Apple Numerics Group, 17 April	1986
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
	EXPORT B2D
B2D	PROC
	pop Argstr	;pointer to decimal	record
	jsr UnPck	;pointer to source (Brgstr)
	pop Crgstr	;pointer to format record
	ldy ZeroWord
	lda [Crgstr],y
	sta style
	lda signB
	sta [Argstr],y
	iny
	iny
	lda [Crgstr],y
	sta digits
	lda Btag
	bpl NzFnVal	;Nonzero Finite Value
	longa off
	longi off
	sep #$30
	eor #$0ff	;negate Btag and subtract 1
	tax
	lda ZINS,x	;get character for zero, infinity or NaN
	ldy #BgnSgDg+1
	sta [argstr],y
	cpx #$02
	lda #$01
	bcc z1s3	;x < 2
	ldx lgthB
	inx
	inx
	stx TmpZP	;lgthB + 2
	ldx #$00
z1s2	lda FrctB+1,x
	lsr a
	lsr a
	lsr a
	lsr a
	jsr Nib2Hex
	lda FrctB+1,x
	and #$0f
	jsr Nib2Hex
	lda FrctB,x
	lsr a
	lsr a
	lsr a
	lsr a
	jsr Nib2Hex
	lda FrctB,x
	and #$0f
	jsr Nib2Hex
	inx
	inx
	cpx TmpZP
	bcc z1s2	;x < lgthB + 2
	tya
	sbc #BgnSgDg	;carry was set
z1s3	ldy #BgnSgDg
	sta [argstr],y
	rep #$30
	jmp TrpHd1	;rts
	sep #$30
Nib2Hex	cmp #$0a
	bcs z2s5	;a > 9
	ora #$30
	bcc z2s6	;always true
z2s5	adc #$56
z2s6	iny
	sta [argstr],y
	rts
	longa on
	longi on
	rep #$30
NzFnVal	lda #ffqq
	sta fence2	;max precision for scaled integer
	lda style
	bne z3s9	;style is fixed
	lda digits
	bmi z3s8	;digits < 0
	bne z3s9	;digits > 0
z3s8	stz digits
	inc digits	;set digits to 1
z3s9	jsr BtoE	;save B, restore if	length > len
	jsr fplog10	;get a floor log10 of source, (ilog)
b2dLoop	sec
	lda style
	bne z4s11	;style is fixed
	lda digits
	sta len	;len := digits
	bcs z4s12	;always true !!!!!!!!!!!!
z4s11	lda ilog
	adc digits
	sta len	;len := ilog + digits + 1
z4s12	bmi z4s13	;len < 0
	sec	;len > SigDigLn ?
	lda #SigDigLn
	sbc len
	bpl z4s13	;no
	lda #SigDigLn
	sta len	;len := SigDigLn
z4s13	clc
	lda len
	sbc ilog
	sta iscale	;iscale := len - ilog - 1
	sta tmpZP
	lda len
	bmi z4s91
	bne z4s92
z4s91	stz len	;len < 1, len := 1
	inc len
z4s92	ldy #$02
	sec
	lda ZeroWord
	sbc iscale
	sta [argstr],y	;exp := ilog + 1 - len (= -iscale)
	jsr ScaleIt
	jsr Rnd2Int
I2ASCII	stz FrctA	;initialize FrctA
	stz lgthA
	lda Ctag
	bmi z5s9	;case of integer zero
	stz tmpZP3	;number of words in	FrctA
	sec
	lda expnC
	sbc #$3fff
	sta tmpZP5
	and #$0f
	sta tmpZP4	;number of bits in last word
	lsr tmpZP5
	lsr tmpZP5
	lsr tmpZP5
	lsr tmpZP5	;location of last word
	stz lgthX	;initialize pointer	to Frctc
	SED	;set decimal mode
z5s2	ldx lgthX
	cpx lgthC	;lgthX - lgthC
	beq z5s3
	bpl z5s4	;assumes tmpZP is '0' from last pass
z5s3	lda FrctC,x	;get next word
	sta tmpZP
z5s4	inc lgthX
	inc lgthX	;set pointer to next word of FrctC
	lda #$0f	;INITIALIZE BIT COUNT
	dec tmpZP5
	bpl z5s5	;do entire word of FrctC
	lda tmpZP4	;do partial word of	FrctC
z5s5	STA tmpZP2	;number of bit shifts
z5s6	LDX ZeroWord
	ldy tmpZP3
	asl tmpZP	;sets carry for add	loop
z5s7	LDA FrctA,X	;ADD LOOP
	ADC FrctA,X
	STA FrctA,X
	inx
	inx
	dey
	bpl z5s7
	BCC z5s8	;NO CARRY PROPAGATION
	stz FrctA,X	;zero new word
	rol FrctA,x	;shift in carry
	STX lgthA	;store new length
	inc tmpZP3	;increment word count
z5s8	DEC tmpZP2
	bpl z5s6
	bit tmpZP5
	bpl z5s2
	CLD	;CLEAR DECIMAL MODE
z5s9
	longa off
	longi off
	sep #$30
	ldy #BgnSgDg	;BEGIN BCD TO ASCII
	sec
	tya
	adc len
	sta tmpZP	;max string location in decimal record
	ldx lgthA
	lda FrctA+1,x
	beq z5s10	;high byte is zero
	inx	;increment x to start with high byte
z5s10	lda #$0f0
	and FrctA,x
	beq z5s13	;leading nibble is zero
z5s11	LDA FrctA,X
z5s12	LSR A
	LSR A
	LSR A
	LSR A
	ora #$30
	INY
	cpy tmpZP
	bcs z5s99	;length (FrctA) > len
	STA [argstr],Y
z5s13	LDA FrctA,X
	and #$0F
	ora #$30
	INY
	cpy tmpZP
	bcs z5s99	;length (FrctA) > len
	STA [argstr],Y
	DEX
	Bpl z5s11
	sec
	TYA
	sbc #BgnSgDg
	ldy #BgnSgDg
	STA [argstr],Y
	longa on
	longi on
	rep #$30
	jmp TrpHd1	;rts
z5s99	rep #$30
;	            ;restore bnm
;      ;R2R     E,B
	jsr EtoB
	stz tmpflgs	;clear flags
	inc ilog	;ilog := ilog + 1
	jmp b2dLoop
	RtoR B,E
	RtoR B,X
	RtoR C,B
	RtoR C,X
	RtoR E,A
	RtoR E,B
	RtoR X,A
	RtoR X,B
	EXPORT ScaleIt
ScaleIt		;       
	bne z6s1
	jmp BtoC	;iscale = 0, jsr & rts
z6s1	sta tmpZP5
	bmi z6s2
	sta iscale	;iscale := -iscale (= exp)
z6s2	lda EnvWrd
	and #$0c000	;mask for rounding direction
	pha	;save rounding mode
;      ;lda     EnvWrd
	asl a
	stz fenceSv	;build a pointer for rounding table
	rol fenceSv	;get rounding direction into pointer
	asl a
	rol fenceSv
	beq z6s3	;rounding mode is round to nearest
	asl tmpZP	;get original signed iscale
	rol fenceSv	;get sign of iscale	into pointer
	lda signB
	lsr a
	rol fenceSv	;get sign of sign into pointer
	rol fenceSv
	ldx fenceSv
	lda RndTbl-8,x
	eor EnvWrd	;alter rounding direction according to
	sta EnvWrd	;the Rounding Table, x
z6s3	jsr ten
	lda signE
	sta signC
	jsr EtoA
	lda #$3fff
	and EnvWrd
	sta EnvWrd
	pla
	ora EnvWrd	;restore rounding direction
	sta EnvWrd
	ldx fence2
	stx fence
	ldy StkBtWd,x	;; tmpZP2 := max (lgthA, MaxShft,x)
	cpy lgthA	;StkBtWd,x - lgthC
	bpl z6s4
	ldy lgthA
z6s4	sty SBWord	;maximum length, i.e., max (lgthA, MaxShft,x)
	bit tmpZP5	;get original signed -iscale
	bpl z6s5	;exp > 0
	jmp mul	;jsr & rts
z6s5	jmp Xdiv	;jsr & rts
FPLog10	lda #$9a1f	;1 less than 2*log2, carry set below
	sta tmpZP
	sec
	lda expnB
	sbc #$3fff	;unbias exponent into twos complement form
	tay
	sta tmpZP2
	sta tmpZP3	;used to generate sign extend below
	bpl z7s1
	inc tmpZP	;increment log2 if unbiased exponent < 0
z7s1	lda FrctB
	sta tmpZP4
	lda ZeroWord
	ldx #$0f
z7s2	lsr tmpZP4	;multiply fraction part times log2
	bcc z7s3
	adc tmpZP	;note carry is set (see comment above)
z7s3	ror a
	dex
	bne z7s2
	ldx #$11
	asl tmpZP3
z7s4	ror tmpZP2	;multiply exponent part times log2
	bcc z7s5
	adc tmpZP	;note carry is set (see comment above)
z7s5	ror a
	dex
	bne z7s4
	tyx
	bpl z7s6
	clc
	sbc tmpZP	;really add of (2^inf - 1)*tmpZP
z7s6	sta ilog	;store ilog
	rts
Ten	lda iscale
	jsr ten15
	lsr iscale
	lsr iscale
	lsr iscale
	lsr iscale
	bne z8s1	;iscale > 15
	rts
z8s1	ldx #ffqq
	stx fence
	lda stkBtWd,x
	sta SBWord
	jsr BtoX
	stz signC	;set sign of result
	lda #$10
	jsr tableX
	jmp z8s4
z8s2	jsr XtoB
	jsr CtoX
z8s3	jsr BtoA
	jsr mul
	jsr ck4URO
	jsr CtoB
z8s4	lsr iscale
	bcc z8s3
	jsr XtoA
	jsr BtoX
	jsr mul
	jsr ck4URO
	lda iscale
	bne z8s2
	jmp CtoB	;rts
ten15	and #$0f
tableX	asl a	;multiply table entry by ten
	sta tmpZP
	asl a
	asl a
;       ;clc	            ;assume	carry is clear
	adc tmpZP
	tay	;and store in y
	stz Btag
	stz signB
	stz expnB+2
	lda XTable+8,y
	asl a
	rol signB
	lsr a
	sta expnB
	ldx #$6
	lda XTable+6,y
	sta FrctB
	lda XTable+4,y
	sta FrctB+2
	lda XTable+2,y
	sta FrctB+4
	lda XTable,y
	sta FrctB+6
	bne z9s2
z9s1	dex
	dex
	lda FrctB,x
	beq z9s1
z9s2	stx lgthB
	rts
	EXPORT XTable
XTable	DC.W 0,$0,$0,$8000,$3FFF
	DC.W 0,$0,$0,$0A000,$4002
	DC.W 0,$0,$0,$0C800,$4005
	DC.W 0,$0,$0,$0FA00,$4008
	DC.W 0,$0,$0,$9C40,$400C
	DC.W 0,$0,$0,$0C350,$400F
	DC.W 0,$0,$0,$0F424,$4012
	DC.W 0,$0,$8000,$9896,$4016
	DC.W 0,$0,$2000,$0BEBC,$4019
	DC.W 0,$0,$2800,$0EE6B,$401C
	DC.W 0,$0,$0F900,$9502,$4020
	DC.W 0,$0,$0B740,$0BA43,$4023
	DC.W 0,$0,$0A510,$0E8D4,$4026
	DC.W 0,$0,$0E72A,$9184,$402A
	DC.W 0,$8000,$20F4,$0B5E6,$402D
	DC.W 0,$0A000,$0A931,$0E35F,$4030
	DC.W 0,$0400,$0C9BF,$8E1B,$4034
	EXPORT Rnd2Int
Rnd2Int		;       
	stz tmpZP2	;least significant bit
	stz tmpZP	;round bit
	sec
	lda expnC	;find the bit location to the right of the
	sbc #$3fff	;lsb, with respect to msb of FrctC
	sta expnX	;store it in expnX
	lda expnC+2
	sbc ZeroWord
	sta expnX+2
	bpl z10s4	;least bit inside left side of FrctC
	ldx ZeroWord	;set word location of first search
	lda #$0ffff
	cmp expnX
	bne z10s10	;lsb miles to the left of msb of FrctC
	cmp expnX+2
	bne z10s10	;lsb to the left of	msb of FrctC
	lda #$8000	;round bit just left of FrctC
	bne z10s8	;check round bit, branch always !!!!!!!
z10s3	rts
z10s4	lda expnX	;make bit mask
	and #$0f
	tax
	sec
	lda ZeroWord
z10s5	ror a
	dex
	bpl z10s5
	sta tmpZP4
	tay	;save mask in y
	lsr ExpnX+2	;calculate word location of lsb
	ror ExpnX
	lsr ExpnX+2
	ror ExpnX
	lsr ExpnX+2
	ror ExpnX
	ror ExpnX
	asl ExpnX
	ldx ExpnX+2
	bne z10s3	;binary point is miles to the right
	ldx ExpnX
	cpx lgthC	;ExpnX - lgthC
	beq z10s7
	bcs z10s3	;decimal point beyond FrctC
z10s7	and FrctC,x
	sta tmpZP2	;save lsb
	tya
	lsr a
	bne z10s8	;not crossing word boundry
	ror a	;crossing a word boundry, take special action
	cpx lgthC	;x - lgthC
	bpl z10s3	;no Round or Sticky	bits
	inx
	inx
z10s8	tay	;check the round bit
	and frctC,x
	sta tmpZP	;save round bit
	eor frctC,x	;clear round bit
	sta frctC,x
	tya	;now deal with the sticky bit
	ldy ZeroWord	;initialize sticky bit container
	dec a
	beq z10s9	;crossing a word boundry
	and frctC,x
	bne z10s11	;sticky bit found
z10s9	cpx lgthC
	bpl done	;x >= lgthC, no sticky bits found
	inx
	inx
z10s10	lda frctC,x
	beq z10s9	;no sticky bit in this word, ck the next one
z10s11	tay	;save sticky bits in y
	eor frctC,x	;clear sticky bits
	sta frctC,x
	tya	;get sticky bits
done	ora tmpZP	;or with round bit
	beq finRTI	;no extra bits
	lda tmpflgs
	ora #inexcp
	sta tmpflgs
	stx lgthC
	lda #$0c000	;mask in rounding direction
	bit EnvWrd
	beq RndNr
	bmi z11s9	;either downward or	toward zero
	lda signC	;round upward
	beq RndUp	;sign is positive
	bne NoRndUp	;no rounding up (in	magnitude)
z11s9	bvs NoRndUp	;toward zero
	lda signC	;round downward
	bne RndUp	;sign is negative
	beq NoRndUp	;no rounding up (in	magnitude)
RndNr	lda tmpZp	;get round bit
	beq NoRndUp	;no round bit, dump	trailing zero words
	tya	;get sticky bits
	ora tmpZP2	;or lsb & sticky bit
	beq NoRndUp	;no rounding up (in	magnitude)
;	    environment word is rr-xdoui RR-XDOUI
;		    rd xcpns rp halts
;               0 to nearest, 1 upward,	2 downward, 3 toward zero
RndUp	ldx ExpnX+2
	bmi rtnOne	;lsb to the left of	FrctC, return 1
	ldx ExpnX	;get word location of round bit
	clc
	lda tmpZP4	;get location of round bit
	adc FrctC,x
	sta FrctC,x
	bcc finRTI
	bcs RUp2
RUpLoop	inc FrctC,x
	bne finRTI
RUp2	dex
	dex
	bpl RUpLoop
	sec
	ror FrctC	;right shift signicand
	inc expnC	;adjust the exponent
	bne finRTI
	inc expnC+2
finRTI	rts
rtnOne	stz expnC+2
	lda #$3fff
	sta expnC
	stz lgthC
	lda #$8000
	sta FrctC
	rts	;tag and sign are OK
NoRndUp	ldx ExpnX+2
	bpl finRTI	;lsb to the left of	FrctC, return 0
	jmp rtnZero
	ENDP
;          copy sane/d2b
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; File:  D2B
;; Description: Decimal --> Binary for ORCA SANE 65816 floating point engine
;; Status: ROM 2.0 and them some.
;;
;; Written by Kenton Hanson, Apple Numerics Group, 16 May 1986
;;
;; Modification History
;;
;;    23 mar 87	  Kenton Hanson
;;
;;       Sticky bit	word error in multiply corrected.
;; 
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
	EXPORT D2B
D2B	PROC
;       ;ldx    fence           ;done in 'fp816'
	stx fence2	;set destination format
	pop Crgstr	;pointer to destination
	pop Argstr	;pointer to decimal	record
	lda rtnA
	pha	;push 'rthArth' as return address
	stz signB
	stz signC
	ldy ZeroWord
	lda [Argstr],y
	beq z1s0
	inc signB
	inc signC
z1s0	ldy #BgnSgDg	;relative location of SigDig in Decimal
	lda [Argstr],y
	and #$0ff
	beq z1s99	;NULL string, return zero
	sta len
	iny
	sty tmpZP	;initialize string pointer
	cmp #SigDigLn+1
	bcc z1s1	;a <= SigDigLn
	lda #SigDigLn
	clc
z1s1	adc #BgnSgDg
	sta tmpZP2	;initialize end location of string in record
	lda [Argstr],y
	and #$0ff
	cmp #$30	;ASCII zero
	bne z1s2
z1s99	jmp rtnZero	;jsr & rts
z1s2	and #$0df
	cmp #$49	;ASCII 'i' or 'I'
	bne z1s22
	jmp RtnInf
z1s22	cmp #$4e	;ASCII 'n' or 'N'
	bne gtdgts
	lda #NaNtag
	sta Ctag
	stz lgthC	;initialize FrctC
	stz FrctC
	ldx lgthC
	inc tmpZP2
	stz tmpZP3	;flag for next word
z1s3	iny
	cpy tmpZP2
	bcs z1s5	;tmpZP >= tmpZP2
	sep #$30
	lda [Argstr],y
	rep #$30
	and #$00ff
	cmp #$41	;'A'
	bcc z1s4	;a < 'A'
	adc #$8	;carry was set, i.e., a := a + 9
z1s4	and #$0f	;get low order nibble
	cpy #BgnSgDg+6
	beq The6th
	asl tmpZP3
	bcs The6th
MoreNaN	asl FrctC,x
	asl FrctC,x
	asl FrctC,x
	asl FrctC,x
	ror tmpZP3	;next word flag
	ora FrctC,x
	sta FrctC,x
	bra z1s3	;always true
The6th	inx
	inx
	stx lgthC
	stz FrctC,x
	inc FrctC,x
	bra MoreNaN
z1s5	txa
	beq z1s7	;1st word, don't left justify
	bit tmpZP3
	bmi z1s7
z1s6	asl FrctC,x
	bcc z1s6
z1s7	lda #$4000	;bit for signaling NaN
	bit FrctC	; *** 21 March 1985	***
	bne z1s8	;not a signaling NaN
	lda FrctC
	jmp InvNaN2	;jsr & rts
z1s8	lda FrctC
	and #$0ff
	bne z1s9	;Non zero NaN code found
	lda #NaNZero
	ora FrctC
	sta FrctC
z1s9	rts
gtdgts	stz lgthB	;initialize FrctB
	stz FrctB
	stz Btag	;intialize tag
	ldy #$2
	lda [Argstr],y
	sta iscale	;get exponent into iscale
	bpl z2s12	;do not need to strip trailing ASCII zeros
z2s3	ldy tmpZP2
	sep #$30
	lda [Argstr],y
	rep #$30
	and #$0f	;change ASCII digit	to binary
	bne z2s12	;no more trailing zeros
	dec tmpZP2
	inc iscale
	bmi z2s3	;iscale still < 0
	bpl z2s12
z2s4	dec iscale	;iscale := iscale -	1
z2s5	lda FrctB
	and #$0e000
	beq z2s6	;no byte shift necessary
	jsr rtWord
	stz FrctB
z2s6	ldx lgthB	;multiply by 10
	clc
z2s7	lda FrctB,x
	rol a
	sta FrctA,x
	sta FrctB,x
	dex
	dex
	bpl z2s7
	ldy #$2
z2s8	ldx lgthB
z2s9	rol FrctB,x
	dex
	dex
	bpl z2s9
	dey
	bne z2s8
	ldx lgthB
z2s10	lda FrctA,x
	adc FrctB,x
	sta FrctB,x
	dex
	dex
	bpl z2s10
	bcc z2s11
	jsr rtWord
z2s11	ldy tmpZP
	cpy tmpZP2
	inc tmpZP
	bcc z2s12	;tmpZP <= tmpZP2
	ldy tmpZP
	bcs z2s14	;always true
z2s12	ldy tmpZP
	sep #$30
	lda [Argstr],y
	rep #$30
	and #$0f	;change ASCII digit	to binary
	ldx lgthB
	clc
z2s13	adc FrctB,x
	sta FrctB,x
	bcc z2s14
	lda ZeroWord
	dex
	dex
	bpl z2s13
	jsr rtWord
z2s14	cpy tmpZP2
	bcc z2s5	;y < tmpZP2
	lda iscale
	beq z2s15	;iscale = 0
	bmi z2s15	;iscale < 0, appending zeros does no good
	cpy #BgnSgDg+SigDigLn
	bcc z2s4	;y < SigDigLn, append zero
z2s15	lda FrctB
	bne z2s17
z2s16	jmp rtnZero	;jsr & rts
z2s17	lda lgthB
	asl a	;multiply by 8
	asl a
	asl a
;      ;clc
	adc #$400e	;add bias (3fff) + 16. - 1
	sta expnB
	stz expnB+2
	bit FrctB
	bmi z2s18	;already normalized
	jsr BgnNrmB
z2s18	jsr BtoE
	lda iscale
	sta tmpZP	;save original iscale
	eor #$0ffff
	inc a	;-iscale
	jmp ScaleIt	;jsr, rts
rtWord	ldx lgthB
	cpx #FrctC-FrctB
	bcs z3s2	;cnm >= 13, don't overwrite memory
	inc lgthB	;increase length of	lgthB
	inc lgthB
z3s1	lda FrctB,x
	sta FrctB+2,x
	dex
	dex
	bpl z3s1
	lda #$1
	sta FrctB
	rts
z3s2	lda #NanAscB	;Invalid dec -> bin	conversion: syntax
	pla	;to prevent returning to digit gathering code
	jmp invNaN	;rts
	EXPORT Mul
Mul		;       
	ldx Atag
	bmi z4s3
	ldy Btag
	bpl BgnMlt	;no special cases
	iny
	beq z4s4	;B is zero and A is	a number, return zero
	bmi z4s6	;B is infinity and A is a number, rtn infinity
z4s3	ldy Btag
	inx
	bmi z4s5	;A is infinity
	iny	;A is zero
	bmi z4s7	;A is zero and B is	infinity, return invalid
;	            ;A is zero and B is not infinity, return zero
z4s4	jmp rtnZero
z4s5	iny
	beq z4s7	;A is infinity and B is zero, return invalid
;	            ;A is infinity and B is non	zero, rtn Infinity
z4s6	jmp rtnInf
z4s7	lda #NanMlt
	jmp InvNan
	EXPORT BgnMlt
BgnMlt		; 
;--------------------------------------------------
; Added 23 Mar 87 to fix problem with multiplies
; of numbers with different formats.  Result was
; not coming back in extended.
; 
	ldx Fence
	ldy stkBtWd,x
	sty SBWord
; 
; End of addition 
;-------------------------------------------------- 
	ldx lgthA
	stx lgthC
	stx tmpZP2
	lsr tmpZP2	;current word count	for FrctC
z5s2	stz FrctC,X	;initialize C
	dex
	dex
	bpl z5s2
z5s3	ldx lgthB
	lda frctB,x
	sta tmpZP
	ldy #$0f
z5s4	lsr tmpZP
	bcc z5s6
	clc
	ldx lgthA
z5s5	lda FrctC,x
	adc FrctA,x
	sta FrctC,x
	dex
	dex
	bpl z5s5
z5s6	lda tmpZP2	;get current word count for FrctC
	ldx ZeroWord
z5s7	ror Frctc,x	;Note, carry is correct for first shift
	inx
	inx
	dec a
	bpl z5s7
	bcc z5s10	;no carry propagation
	ldx lgthC
	cpx SBWord	;lgthC - SBWord
	bcc z5s8	;lgthC < SBWord
	lda FrctC,x
	ora #$1	;or carry out, into	last bit of FrctC,x
	bne z5s9	;always true
z5s8	inx	;add new word with a leading bit set
	inx
	stx lgthC
	inc tmpZP2	;update word length
	lda #$8000
z5s9	sta FrctC,x
z5s10	dey
	bpl z5s4
	dec lgthB
	dec lgthB
	bpl z5s3
	clc
	lda expnA
	adc expnB
	sta expnC
	lda expnA+2
	adc expnB+2
	sta expnC+2
	lda FrctC
	asl a
	bcs z5s13	;leading bit set, no left shift necessary
	ldx lgthC
	clc
z5s12	rol FrctC,x
	dex
	dex
	bpl z5s12
z5s13	lda expnC	;adjust exponent
	sbc #$3ffe
	sta expnC
	lda expnC+2
	sbc ZeroWord
	sta expnC+2
z5s15	ldx lgthC	;strip trailing zero word loop
	lda FrctC,x
	bne z5s20
	dec lgthC
	dec lgthC
	bpl z5s15
z5s20	rts
	EXPORT DnrmC
DnrmC		;       
	ldx fence
	lda expnX+2	;contains hi word of number of right shifts
	bne z6s2	;do max shift
	lda expnX	;expnX contains low	word of # of right shifts
	bne z6s1
	rts	;no shifting
z6s1	cmp MaxShft,x
	bcc z6s3	;a < MaxShft,x
z6s2	lda MaxShft,x	;stuff maximum shift into expnX
	sta expnX
z6s3	ldy StkBtWd,x	;; tmpZP2 := max (lgthC, MaxShft,x)
	cpy lgthC	;StkBtWd,x - lgthC
	bpl z6s4
	ldy lgthC
z6s4	sty tmpZp2	;maximum length, i.e., max (lgthC, MaxShft,x)
BitShft	stz tmpZP3	;left storage word for bit shift
	lda expnX	;number of right shifts
	and #$0f
	beq WrdShft	;no bit shift
	cmp #$9
	bcc z7s3	;a < 9
z7s1	ldx lgthC
	clc
z7s2	rol FrctC,x
	dex
	dex
	bpl z7s2
	rol tmpZP3
	inc expnX
	lda expnX
	and #$0f
	bne z7s1
	beq WrdShft
z7s3	ldx ZeroWord
	clc
z7s4	ror FrctC,x
	txa
	eor lgthC
	beq z7s5
	inx
	inx
	bne z7s4	;branch always  branch always  branch always
z7s5	bcc z7s9
	cpx tmpZP2
	bpl z7s7	;x >= tmpZP2 (i.e.,	StkBtWd,x)
	inx
	inx
	stx lgthC
	lda #$8000
	bne z7s8	;branch always  branch always  branch always
z7s7	lda FrctC,x
	ora #$1	;or in sticky bit
z7s8	sta FrctC,x
z7s9	dec expnX
	lda expnX
	and #$0f
	bne z7s3
WrdShft	stz tmpZP4	;bucket for sticky words
	lda expnX	;number of bits to be shifted
	lsr a
	lsr a
	lsr a
	lsr a
	beq ckTail	;no word shift
	tay	;number of word shifts
z8s3	ldx lgthC
	cpx TmpZP2	;x - MaxlgthC
	bmi z8s4	;x < MaxlgthC
	lda FrctC,x
	beq z8s6
	sta tmpZP4	;sticky word found,	save it
	bne z8s6
z8s4	inc lgthC
	inc lgthC
z8s5	lda FrctC,x	;right shift one word loop
	sta FrctC+2,x
z8s6	dex
	dex
	bpl z8s5
	lda tmpZP3	;on first pass use original value
	sta FrctC
	stz tmpZP3	;thereafter, fill from the left with zero words
	dey
	bne z8s3
ckTail	lda tmpZP4
	beq z9s7	;nothing in sticky word bucket
	ldx lgthC
	lda FrctC,x
	ora #$1	;insert sticky bit
	sta FrctC,x
	bne z9s8	;branch always
z9s7	ldx lgthC
	lda FrctC,x
	bne z9s8
	dec lgthC
	dec lgthC
	bpl z9s7
z9s8	rts
	ENDP
;          copy sane/dcstr
	EXPORT DecStr
DecStr	PROC
	jsl IncBusyFlg
	phd	; put direct reg on	stack
	tcd	; set direct reg
	pla	; save orig direct on zp
	sta OrigDirect
	phb	; save original data bank register
	phb
	pla
	sta OrigBank	; on zero page
	phk	; set data bank register
	plb	; this bank
	pla
	sta ZPRtn
	pla
	sta ZPRtn+2
	pla
	sta ZPRtn+4
FOPSTR2DEC	EQU 0	; Pascal string to decimal record
FOCSTR2DEC	EQU 2	; C string to decimal record
FODEC2STR	EQU 1	; decimal record to	string
;      ;.proc   DecStr
;      ;.ref    Str2Dec,Dec2Str,CStr2Dec
	plx	;put opcode into 'x'
	lda rtnB	;address of XitFP
	pha
	cpx #FODEC2STR	;opcode - #FODEC2STR
	bpl z1s3
	jmp Str2Dec
z1s3	bne z1s4
	jmp Dec2Str
z1s4	jmp CStr2Dec
	ENDP
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; File: NewD2S.TEXT
;; 6502 number formatter -- implements Pascal Dec2Str
;; Status: ALPHA
;; Copyright Apple Computer, Inc. 1985
;;
;; Written by Kenton Hanson, Apple Numerics Group, 8 March 85
;; 
;; 8 May 86, KLH modified for Cortland Orca assembler
;;      ___________________________________________________________
;;
;; Description: This code is intended to be position independent
;;
;;
;; Const
;;    DECSTRLEN = 80;
;;    SigDigLen = 28;
;; type
;;    DecStr   = string [DECSTRLEN];
;;    Decimal  = record
;;	sgn : 0..1;
;;	exp : integer;
;;	sig : string [SigDigLen]
;;               end;
;;    DecForm = record
;;	style  : (FLOATDecimal, FIXEDDecimal);
;;	digits : integer
;;               end;
;;
;;
;; Procedure Dec2Str (f : DecForm; d : decimal; var y : DecStr) ; external;
;;
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
	EXPORT Dec2Str
Dec2Str	PROC
wmax	equ 80
wmaxP1	equ wmax+1
wmaxP4	equ wmax+4
temp	equ 128
fstyle	equ temp
fdgts	equ temp+1
dst	equ temp+2
src	equ temp+6
BCD	equ temp+6	; holds BCD intermediate result (3 bytes)
DecFrm	equ temp+10
sgn	equ DecFrm
MinDig	equ temp+11	; minus Minimum digits for NaN or Exponent
exp	equ DecFrm+2
dsig	equ DecFrm+4
frstdig	equ DecFrm+5
	ldx #$00
z20s0	pla	; pull return and arguments from stack
	sta temp,x
	inx
	inx
	cpx #$0e
	bne z20s0
	lda temp	; push return address back onto stack
	pha
	longa off
	longi off
	sep #$30
	ldy #$00
	lda [DecFrm],y	; get DecForm into memory
	sta fstyle
	ldy #$02
	lda [DecFrm],y
	sta fdgts
	iny
	lda [DecFrm],y	; check that fdgts is a valid signed byte
	bmi z20s1
	bne ErrXitJ1	; fdgts > 255
	lda fdgts
	bpl z20s2
	bmi ErrXitJ1	; fdgts > 127
z20s1	eor #$0ff
	bne ErrXitJ1	; fdgts < -256
	lda fdgts
	bpl ErrXitJ1	; fdgts < -128
z20s2	ldy #$5
z20s3	lda [src],y	; get first 6 bytes	of Decimal Rec. into memory
	tyx
	sta sgn,x
	dey
	bpl z20s3
	lda dsig	; get length byte
	beq ErrXitJ1	; length = 0 (error)
	cmp #$1d	; a - 29.	    <klh 18oct85>
	bcs ErrXitJ1	; a >= 29.
SgnBlnk	ldy #$00
	sec
	lda #$2d	; .ascii  '-'
	bit sgn	; NOTE: bit instruction does not set 'carry'
	bne z21s2	; Obscure, #2d is odd, & has been anded	with sgn
	lda fstyle
	bne z21s3	; Fixed & postive, no leading character
	lda #$20	; .ascii  ' '
z21s2	clc
	iny
	sta [dst],y
z21s3	lda #$03	; offset address of	src by 4 or 3
	adc src
	sta src
	bcc z21s99
	inc src+1
	bne z21s99
	inc src+2
	bne z21s99
	inc src+3
z21s99	lda frstdig
	cmp #$3f	; '?'
	beq ErrXitJ1
	cmp #$30	; '0'
	bne z21s4
	ldx #$00
	stx exp	; exp <- 0
	stx exp+1
	inx
	stx dsig	; length (sig) <- 1
	bne Num	; branch always
;  BRANCH ALWAYS   BRANCH ALWAYS   BRANCH ALWAYS   BRANCH ALWAYS   BRANCH ALWAYS
z21s4	and #$0df	; turn off upper/lower case bit
	cmp #$4E	; .ascii  'N' or 'n'
	beq NaN1	; NAN
	cmp #$49	; .ascii  'I' or 'i'
	bne Num	; zero, normal or denormal number
	lda #$49	; 'I'
	iny
	sta [dst],y
	lda #$4e	; 'N'
	iny
	sta [dst],y
	lda #$46	; 'F'
	iny
	sta [dst],y
	bne Fin1	; branch always
;  BRANCH ALWAYS   BRANCH ALWAYS   BRANCH ALWAYS   BRANCH ALWAYS   BRANCH ALWAYS
ErrXitJ1	lda #$3f	; '?'
	ldy #$01
	sta [dst],y
	tya
	dey
Ex1	sta [dst],y
	rep #$30
	rts
Fin1	tya
	ldy #$00
	beq Ex1	; branch always
;  BRANCH ALWAYS   BRANCH ALWAYS   BRANCH ALWAYS   BRANCH ALWAYS   BRANCH ALWAYS
Num	ldx fstyle
	bne BgnFxd
;           ;if f.digits < 1 then f.digits := 1;         { ErrXit ???? }
;           ;s := concat (s, copy (d.sig, 1, 1));
;           ;if (f.digits > 1) or (length (d.sig) > 1) then
;              ;begin
;                 ;s := concat (s, '.');
;                 ;if length (d.sig) > 1 then
;	     ;s := concat (s, copy (d.sig, 2, length (d.sig) - 1));
;                 ;if f.digits > length	(d.sig) then
;	                   ;AppendZeros (f.digits - length (d.sig));
	iny	; copy one digit before the '.'
	lda [src],y
	sta [dst],y
;           ;if (f.digits > 1) or (length (d.sig) > 1) then
	dec dsig	; dsig := dsig - 1
	lda fdgts
	cmp #$02	; fdgts - 2
	bpl z22s1	; fdgts > 1
	lda #$01
	sta fdgts
	ldx dsig
	beq z22s5	; dsig = 1
z22s1	lda #$2e	; '.'
	iny
	sta [dst],y
	ldx dsig
	beq z22s5
z22s3	lda [src],y	; copy after the '.'
	iny
	sta [dst],y
	dex
	bne z22s3
z22s5	lda #$0ff
	eor dsig	; a := -dsig
	bmi FromFlt	; branch always
;  BRANCH ALWAYS   BRANCH ALWAYS   BRANCH ALWAYS   BRANCH ALWAYS   BRANCH ALWAYS
Err1	clc
	bcc ErrXitJ1
NaN1	beq NaN2
BgnFxd	lda exp+1	; check that exp is	a valid signed byte
	bmi z23s1
	bne ErrXitJ1	; exp > 255
	lda exp
	bpl BF2
	bmi ErrXitJ1	; exp > 127
z23s1	eor #$0ff
	bne ErrXitJ1	; exp < -256
	lda exp
	bpl ErrXitJ1	; exp < -128
BF2	lda exp
	clc
	adc dsig
	bvs ErrXitJ1
	tax
	bmi AllAftr
	beq AllAftr
	lda exp
	clc
	bpl Before
	adc dsig
z24s1	iny	; copy before the '.'
	lda [src],y
	sta [dst],y
	dex
	bne z24s1
	lda #$2e	; '.'
	iny
	sta [dst],y
	ldx exp
z24s2	lda [src],y	; copy after the '.'
	iny
	sta [dst],y
	inx
	bmi z24s2
	bpl ApZ	; branch always
;  BRANCH ALWAYS   BRANCH ALWAYS   BRANCH ALWAYS   BRANCH ALWAYS   BRANCH ALWAYS
Before	ldx dsig
z25s4	iny	; copy all dsig before the '.'
	lda [src],y
	sta [dst],y
	dex
	bne z25s4
	tya
	adc exp	; carry cleared above
	bvs Err1
	cmp #wmaxP1	; a - wmaxP1
	bpl Err1
	ldx exp
	beq z25s6
	lda #$30	; '0'
z25s5	iny
	sta [dst],y
	dex
	bne z25s5
	stx exp
z25s6	cpx fdgts	; x - fdgts
	bpl ApZ	; fdgts <= 0
	cpy #wmax	; y - wmax
	bcs ErrXit
	lda #$2e	; '.'
	iny
	sta [dst],y
	bne ApZ	; branch always
;  BRANCH ALWAYS   BRANCH ALWAYS   BRANCH ALWAYS   BRANCH ALWAYS   BRANCH ALWAYS
FromFlt	bmi FFlt
NaN2	beq NaN3
AllAftr	dex
	txa
	inx
	clc
	adc src
	sta src
	lda #$0ff
	adc src+1
	sta src+1
	lda #$30	; '0'
	iny
	sta [dst],y
	lda #$2e	; '.'
	iny
	sta [dst],y
	txa
	beq z26s16
	lda #$30	; '0'
z26s15	iny
	cpy #wmaxP1	; a - wmaxP1
	bcs ErrXit
	sta [dst],y
	inx
	bne z26s15
;; adjust the address of source, possibly restructuring ErrXit above
z26s16	tya
	clc
	adc dsig
	cmp #wmaxP1	; a - wmaxP1
	bpl ErrXit
	ldx dsig
z26s17	lda [src],y	; copy all dsig after the '.'
	iny
	sta [dst],y
	dex
	bne z26s17
;	;if d.exp >= -f.digits then AppendZeros	(f.digits + d.exp);
ApZ	lda exp
FFlt	clc
	adc fdgts
	bvs ErrXit	;
	bmi fine
	beq fine
	tax
	sty temp+1
	clc
	adc temp+1
	cmp #wmaxP1	; a - wmaxP1
	bpl ErrXit
	lda #$30	; '0'
z27s9	iny
	sta [dst],y
	dex
	bne z27s9
fine	ldx fstyle
	beq BgnExp
fin	tya
	ldy #$00
	sta [dst],y
	rep #$30
	rts
ErrXit	lda #$3f	; '?'
	ldy #$01
	sta [dst],y
	bne fin
;  BRANCH ALWAYS   BRANCH ALWAYS   BRANCH ALWAYS   BRANCH ALWAYS   BRANCH ALWAYS
;;------------------------------------------------------------------------------
;;
;; change unsigned binary number in exp	into a BCD number in BCD to BCD+2
;;
;;------------------------------------------------------------------------------
NaN3	sta frstdig
	iny
	sta [dst],y
	lda #$41	; 'A'
	iny
	sta [dst],y
	sty MinDig
	lda #$4e	; 'N'
	iny
	sta [dst],y
	lda #$28	; '('
	iny
	sta [dst],y
	sty temp+1	; save y
	iny
	iny
	iny
	lda #$29	; ')'
	iny
	sta [dst],y
	tya
	ldy #$00
	sta [dst],y
	tya	; initialize exp
	ldy MinDig	; restore y
	ldx dsig
	cpx #$06	; y - wmax
	bcc z28s3	; x < 6
	ldx #$05
z28s3	dex
	beq z28s5
	asl a
	asl a
	asl a
	asl a
	sta exp
	lda [src],y
	cmp #$40	; a - #40
	bcc z28s4	; not a letter
	adc #$08
z28s4	and #$0f
	ora exp
	iny
	bne z28s3
;  BRANCH ALWAYS   BRANCH ALWAYS   BRANCH ALWAYS   BRANCH ALWAYS   BRANCH ALWAYS
;      ;ldx     #00
z28s5	sta exp
	lda #$0fc	; -4
	bne codeNaN	; branch always
;  BRANCH ALWAYS   BRANCH ALWAYS   BRANCH ALWAYS   BRANCH ALWAYS   BRANCH ALWAYS
;           ;STR (d.exp + length (d.sig) - 1, SExp);     { no check for wrap }
;           ;if SExp [1] <> '-' then SExp := concat ('+', SExp);
;           ;if (length (s) + length (SExp)) < Wmax then
;		             ;s := concat (s, 'e', SExp)
;		           ;else ErrXit;
BgnExp	cpy #wmaxP4	; a - wmaxP4
ErrXit3	bcs ErrXit
	lda #$65	; 'e'
	iny
	sta [dst],y
;      ;ldx     #00	            ; set above by 'ldx     fstyle'
;      ;clc	            ; reset	    by 'cpy     #wmaxP4
	lda dsig
	adc exp
	sta exp
	txa
	adc exp+1
	sta exp+1
	bvs ErrXit	; exponent overflows integer value
	bmi z29s1
	lda #$2b	; '+'
	bne z29s2	; branch always
z29s1	sec
	txa
	sbc exp
	sta exp
	txa
	sbc exp+1
	sta exp+1
	lda #$2d	; '-'
z29s2	iny
	sta [dst],y
	lda #$0fa	; -6
	inx
	sty temp+1	; save y
codeNaN	sta MinDig
	lda #$00
	sta BCD
	sta BCD+1
	sta BCD+2
	sed
z30s0	ldy #$08	; number of valid bits per byte
z30s1	asl exp,x
	lda BCD
	adc BCD
	sta BCD
	lda BCD+1
	adc BCD+1
	sta BCD+1
	lda BCD+2
	adc BCD+2
	sta BCD+2
	dey
	bne z30s1
	dex
	bpl z30s0
	cld
	ldy temp+1	; restore y
N2Ascii	ldx #$2
z31s1	lda BCD,x
	lsr a
	lsr a
	lsr a
	lsr a
	beq z31s2	; zero encountered
	sta MinDig
z31s2	inc MinDig
	bmi z31s4
	ora #$30
	iny
	cpy #wmaxP1	; y - wmaxP1
z31s3	bcs ErrXit3
	sta [dst],y
z31s4	lda BCD,x
	and #$0f
	beq z31s5	; zero encountered
	sta MinDig
z31s5	inc MinDig
	bmi z31s6
	ora #$30
	iny
	cpy #wmaxP1	; y - wmaxP1
	bcs z31s3	; ErrXit3
	sta [dst],y
z31s6	dex
	bpl z31s1
	lda frstdig
	cmp #$4e
	beq rts3
	tya
	ldy #$00
	sta [dst],y
	longa on
	longi on
rts3	rep #$30
	rts
	ENDP

	EXPORT CStr2Dec
CStr2Dec	PROC
;          copy sane/str2dec1
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; File:  Str2Dec.TEXT
;; Decimal string scanner for 6502
;; Status: BETA
;; Copyright Apple Computer, Inc. 1984,1985
;;
;; Written by Clayton Lewis, Apple Numerics Group, 27 September 1984
;;
;; Modification History:
;;    14 May 84  JTC  Original 68000 code named ScanDec
;;    03 Oct 84  CRL  CStr2Dec entry point for character streams
;;    18 Oct 84  CRL  Clean and compact	the code
;;    05 Nov 84  CRL  No longer accept Ascii with high bit set
;;	  Signalling NaN delivered if no characters accepted
;;    15 Jan 85  RwH  (Think) changed .includes for my directory structure.
;;	  changed .org to XORG.  Added XEND at end.
;;    03 Feb 85  rwh  changed % to ? in	macros...added .ref	FP6502
;;    15 feb 85  rwh  deleted .ref FP6502 - its back to being in Think's ORGS
;;	  file.
;;    16 mar 85  rwh  added .nolists for faster assembly
;;    17 May 85  CRL  Bug in State2.  The string '0)' left index in limbo.
;;	  Fix in State2 is marked by  {add 4 lines 17 May 85 CRL}
;;    23 May 85  CRL  Fold in ongoing changes: AppendDigit to ignore characters
;;	  after 28th,(13 Mar 85).  NaN signalling bit change at SNaN
;;	  and State99 for 68881 and 68000 compatibility, (19 Mar 85)
;;    19 Jun 85  CRL  Change constant from 2 to 4 in 'State99, process NaNcode'.
;;	  Fixes bug in IP; nancode now in correct byte.
;;    11 Jul 85  CRL  Bug in state6.  '..' handled wrong.  Fix moves local label
;;	  $1 up by three lines, replaces LDA with LDX.
;;    29 Aug 85  klh  Butchered to assemble on the ///.
;;    18 Oct 85  klh  More mutilation to assemble under ORCA/M.
;;     7 May 86  klh  Altered to run in	65816 native mode with 8-bit registers.
;;              ________________________________________________
;;
;; Define storage on zero page and set switches for machine/environment:
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

ADDRESSINDIRECT	EQU 128	; start of zero page use
REGISTERS	EQU 160
VP	EQU ADDRESSINDIRECT
D	EQU ADDRESSINDIRECT+4
INDEX	EQU ADDRESSINDIRECT+8
thisS	EQU ADDRESSINDIRECT+12
DSIGLOC	EQU ADDRESSINDIRECT+16
CHARPTR	EQU REGISTERS	; ^char interface needs 2 bytes
SAVEDCHARPTR	EQU REGISTERS+2
LASTCHARPTR	EQU REGISTERS+4
NaNCODE	EQU REGISTERS+5
FLZERO	EQU REGISTERS+6
FLVALPREF	EQU REGISTERS+7
FLEXPSGN	EQU REGISTERS+8
STACKADJ	EQU REGISTERS+9
DSIGLENGTH	EQU REGISTERS+10
EXPONENT	EQU REGISTERS+11
EXPADJUST	EQU REGISTERS+13
STMP	EQU REGISTERS+15
CSTRING	EQU REGISTERS+16
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; Define constants.
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
SIGDIGLEN	EQU 28	; note: 20 for the 68000
TAB	EQU $09
BLANK	EQU $20
OPENPAREN	EQU $28
CLOSEPAREN	EQU $29
PLUS	EQU $2B
MINUS	EQU $2D
DOT	EQU $2E
ZERO	EQU $30
ONE	EQU $31
FOUR	EQU $34	; {addition 23 May 85  CRL}
NINE	EQU $39
COLON	EQU $3A
BIGA	EQU $41
E	EQU $45
F	EQU $46
I	EQU $49
N	EQU $4E
SMALLa	EQU $61
SMALLe	EQU $65
zPLUS1	EQU $7B
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; Start of routine, note the 2 entry points:  Str2Dec and CStr2Dec.
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
	LDX #$0
	JMP Start2
	EXPORT Str2Dec
Str2Dec		;       
	LDX #$1
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; Save items passed on the stack and initialize flags.
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
start2	STX CSTRING	; no length byte if	CSTRING is 0
	ply
	LDX #$0	; first save 16 bytes from stack
z2s1	PLA
	STA ADDRESSINDIRECT,X
	INX
	inx
	CPX #16
	BCC z2s1
	phy
	LDX #$10	; 16 slots to zero
z2s2	DEX
	dex
	STz REGISTERS,X
	BNE z2s2
	TxY	; putting 0 into Y
	longa off
	longi off
	sep #$30
	LDA [INDEX],Y
	STA CHARPTR	; offset from start	of S
	STA SAVEDCHARPTR
	LDX CSTRING
	BEQ z2s3
	LDA [thisS],Y	; length byte of S
	STA LASTCHARPTR	; offset from start	of S
	INY	; Y now has 1
	LDA [INDEX],Y	; look at hi byte of index
	BEQ z2s5	;    OK if zero
	JMP StateFin	;    exit if index too large
z2s3	INY	; keep the hi byte of index also
	LDA [INDEX],Y
	STA CHARPTR+1
	STA SAVEDCHARPTR+1
	CLC	; then advance the same number of
	ADC thisS+1	;    pages into the	buffer S so
	STA thisS+1	;    Y can index into next page
z2s5	CLC	; set up pointer to	d.sig
	LDA D	;    4 bytes into D
	ADC #$4
	STA DSIGLOC
	LDA D+1
	ADC #$0
	STA DSIGLOC+1
	LDA D+2
	ADC #$0
	STA DSIGLOC+2
	LDA D+3
	ADC #$0
	STA DSIGLOC+3
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; Initialize the decimal record to sign +, EXPONENT 0 and significand N0011.
;;      Use SNaN = 00 00 00 00 05 4E 30	30 31 31  = (0)(0)(0)(0)(5)'N0011'
;;      {change only to comment: 23 May	85 CRL, signaling NaN bit change}
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
InitDec	LDY #$9
z3s1	LDA SNaN,Y	; data for SNaN at end of file
	STA [D],Y
	DEY
	BPL z3s1
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; State0 -- scan leading white space.	Blanks and tab characters.
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
State0	JSR GetChar
	CMP #BLANK	; blank
	BEQ State0
	CMP #TAB	; tab
	BEQ State0
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; Then fall through to State1 looking for a sign.
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
State1	CMP #PLUS
	BEQ z4s1	; +, go on since d.sgn already = 0
	CMP #MINUS
	BNE State2	; not +/-, check for number
	LDA #$1	; -, so store 0001 in d.sgn
	LDY #$0
	STA [D],Y
	TYA
	INY
	STA [D],Y
z4s1	JSR GetChar
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; Now expect to have INF, NaN, or number.
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
State2	CMP #ZERO	; look for leading zeros
	BCC z5s1	;    character < '0'
z5s0	BNE z5s2	;    character > '0'
	SEC
	ROL FLZERO	; zero seen, set lo	bit of FLZERO
	JSR ClearSavedPtr	; use current scan pointer
	JSR GetChar	; try again with a new character
	BCS z5s0	; branch if char is	'0' or greater
z5s1	CMP #DOT	; only interesting character < 0
	BEQ State6	; dot found before signif digits
;       ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;       ;; {Add 4 lines 17 May 85 CRL}
;       ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
	LDA FLZERO
	BEQ z5s3
	JMP StateNum
z5s3	JMP StateBackup	; illegitimate number
z5s2	CMP #COLON
	BCC State4	; digits follow, go	scan them
	LDY FLZERO	; any leading zero's?
	BNE GoTo8	;    if so, try E,e
	JMP State99	;    if not, try INF, NaN
GoTo8	JMP State8
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; State4 -- Scan significant digits before the decimal point.  Uses
;;      AppendDigit	to place digit in string, get next character, and
;;      compare next character to 0.
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
State4	JSR ClearSavedPtr	; use current scan pointer
z6s1	JSR AppendDigit
	BCC State5	; character < '0', so look for dot
	CMP #COLON	; one greater than '9'
	BCC z6s1	; =< 3A,   so character in 0..9
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; State5 -- check for start of fraction field after some significant
;;      digits have	already been scanned.
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
State5	CMP #DOT
	BNE GoTo8	; try EXPONENT field (State8)
	JSR GetChar
	BCS z7s1	; potential digits
	JMP StateNum	; dot followed by no digits, end
z7s1	JMP State75
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; AppendDigit
;;      - Place numeral in digit field of decimal
;;      - Get next character
;;      - Compare with '0' before returning
;;        {remove last comment: 13 Mar 85 CRL}
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
AppendDigit	LDY DSIGLENGTH
	CPY #SIGDIGLEN	; past all valid digits?
	BCS z8s3	;    if so, just count lost digits
	INY	;    if not, advance pointer
	STY DSIGLENGTH	;    and store digit in d.sig
	STA [DSIGLOC],Y
	BNE GetChar	; always
;{13 Mar 85 CRL}	   ORA     @DSIGLOC,Y   ; OR into 28th digit
;{13 Mar 85 CRL}	   STA     @DSIGLOC,Y
z8s3	INC EXPADJUST	; record the # of lost digits
	BNE GetChar
	INC EXPADJUST+1
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; GetChar
;;      - STACKADJ <-- # bytes of return addresses on stack
;;      - Check for	end of scanning string
;;      - Collect next character (if not at string end)
;;      - Set ValidPrefix to TRUE if at	string end
;;      - See if saved instead of current pointer should be	returned
;;
;; Note the two entry points.  GETCHAR2	is called by GETMAPPED with 4 in A.
;;      Since GETMAPPED calls via JSR, there are two return	addresses
;;      to pop if scan ends prematurely	and we do not return to caller.
;;      Other calls	need to pop only one return address.
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
GetChar	LDA #$2	; stack adjustment if abort
GetChar2	STA STACKADJ
	JSR CheckEndScan	; end of scan?
	BCC z9s7	;    if so, branch
	LDY CHARPTR	;    else get next character
	LDA [thisS],Y
	INC CHARPTR
	BNE z9s5
	INC CHARPTR+1
	INC thisS+1
z9s5	CMP #ZERO
	RTS
z9s7	INC FLVALPREF	; premature string end
	LDY STACKADJ	; pop return address(es)
z9s9	PLA
	DEY
	BNE z9s9
	LDY SAVEDCHARPTR+1	; has a pointer been saved?
	BMI z9s11	;    if not, just exit
	JMP StateBackup	;    else reset ptr, then exit
z9s11	JMP StNum2
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; State6 -- encountered dot before any	nonzero significant	digits.  Keep
;; looking for digits, but throw away leading zeros, remembering placement
;; of decimal.  If possible nonzero digit or letter, skip right into fraction
;; digit scanner.
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
State6	JSR GetChar	; skip past dot
	BEQ z10s3	; if zero, branch
	BCC z10s1	; if less than zero, end
	CMP #COLON	; > zero, but is it	9 or less?
	BCC State7	;    if 1..9, next state
;       ;; Local label $1 moved to next	line from previous location
;       ;;   4 lines down between two JMP instructions.
;       ;; Change next instruction to LDX instead of LDA.	11 July 85  CRL
z10s1	LDX FLZERO	; no digits, were there 0's?
	BNE State8	;    if so, try E,e
	JMP StateBackup	;    else return to	begin of scan
	JMP StateNum
z10s3	SEC
	ROL FLZERO	; character = '0', set flag
	JSR ClearSavedPtr	; use current character pointer
	LDX expadjust
	BNE z10s12
	DEC expadjust+1
z10s12	DEC expadjust
	BNE State6	; and look for more	0's  (always)
;          copy sane/str2dec2
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; State7 -- scan fraction digits.  Use	AppendDigit and keep count of the
;; number of digits	scanned, whether in	excess of 28 or not.
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
State7	JSR ClearSavedPtr	; use current character pointer
	LDX expadjust	; count number of fraction digits
	BNE z11s12
	DEC expadjust+1
z11s12	DEC expadjust
	JSR AppendDigit
	BCS State75	; if > zero, keep looking
	JMP StateNum	;    else done
State75	CMP #COLON	; exit if character	> '9'
	BCC State7
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; State8 -- look for EXPONENT: E or e followed be optional	sign and a digit
;; field.  Must be careful about EXPONENT overflow.  Since the extended
;; range admits values in magnitude between 10^-5000 and 10^5000, it
;; suffices to check for EXPONENT outside 4096 + 1024 = 5120.  Overflowed
;; EXPONENT are mapped into $1400 (5120), guaranteed to produce a severe
;; overflow on conversion.
;;
;; Must also guard against overscan of expressions like '123e-'.  We must
;; return 123 with pointer at 'e'.  If E or e is found, hold input pointer
;; in case subsequent scanning does not	make sense of the characters past
;; '123'.
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
State8	JSR UpdatePtr	; save current character pointer
	LDX savedcharptr	; and move it back by one
	BNE z12s12
	DEC savedcharptr+1
z12s12	DEC savedcharptr
	CMP #E
	BEQ z12s1
	CMP #SMALLe
	BNE z12s15	; no EXPONENT found
z12s1	JSR GetChar
	CMP #PLUS
	BEQ z12s3
	CMP #MINUS
	BNE z12s5
	INC FLEXPSGN	; mark as negative EXPONENT
z12s3	JSR GetChar	; skip past sign character
z12s5	CMP #ZERO	; >= '0'?
	BCC z12s15	;   if not, end digit field
	CMP #COLON	; =< '9'?
	BCS z12s15	;   if not, end  digit field
	JSR ClearSavedPtr	; digit, use current char pointer
	AND #$0F	; mask to nibble
	TAX	; hold digit in X until end of loop
;          ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;          ;; Multiply EXPONENT by 10
;          ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
	ASL EXPONENT	; first multiply by	2
	ROL EXPONENT+1	;   EXPONENT word *	2
	LDA EXPONENT+1
	STA STMP	; copy to A register and STMP
	LDA EXPONENT	;   then multiply those by 4
	ASL A	;   to get EXPONENT	* 8
	ROL STMP
	ASL A
	ROL STMP	; note that carry is not set so...
	ADC EXPONENT	; now add Exp * 2 +	Exp * 8
	STA EXPONENT	;    to get EXPONENT * 10
	LDA STMP
	ADC EXPONENT+1
	STA EXPONENT+1
;          ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;          ;; And add in the current digit.
;          ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
	CLC
	TXA	; pick up digit from X
	ADC EXPONENT
	STA EXPONENT
	BCC z12s9
	INC EXPONENT+1
z12s9	LDA #$14
	CMP EXPONENT+1	; EXPONENT >= 5120?	(overflow)
	BCS z12s13	;    if not, continue
	STA EXPONENT+1	;    else just use 5120
z12s13	JMP z12s3
;       ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;       ;; Finish scan by checking to see whether we have overscanned.
;       ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
z12s15	LDY SAVEDCHARPTR+1	; overscan?
	BMI StateNum	;    if not, go on
	JMP StateBackup	;    if so, reset pointer
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; StateNum -- have	sighted the first unrecognizable character after a
;; possible number.	 Check first for 0,	a special case since all digits
;; were discarded above, then put the number in canonical form.
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
StateNum	LDX charptr
	BNE z13s12
	DEC charptr+1
z13s12	DEC charptr
StNum2	LDY #$0	; needed at $2 and 5 lines down
	LDA DSIGLENGTH	; any digits?
	BNE z14s2	;    if so, branch
	LDA FLZERO	;    if none, 0?
	BEQ StateFin	;       not even 0!! (branch always)
	LDA #$1	; if 0, then return	d.sig = 0
	STA [DSIGLOC],Y	; length byte of 1 in d.sib
	INY
	LDA #$30	; then the '0' character
	STA [DSIGLOC],Y
	BNE StateFin	; always
;       ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;       ;; Have nonzero number with:
;       ;;      - DSIGLENGTH digits
;       ;;      - Positive binary exponent in EXPONENT, EXPONENT+1
;       ;;      - Lost digit count minus fraction digit count in EXPADJUST
;       ;;
;       ;; To align	decimal point at the end of d.sig, compute:
;       ;;      true exponent := EXPONENT + EXPADJUST.
;       ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
z14s2	STA [DSIGLOC],Y	; put length byte in d.sig
	LDA FLEXPSGN	; negative exponent?
	BEQ z14s5	;    if not, branch
;       ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;       ;; Negate the integer EXPONENT.
;       ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
	SEC
	LDA #$0
	SBC EXPONENT
	STA EXPONENT
	LDA #$0
	SBC EXPONENT+1
	STA EXPONENT+1
;       ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;       ;; Finish up with the final addition.
;       ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
z14s5	LDY #$2	; d.exp occupies bytes 2 & 3 of D
	CLC
	LDA EXPONENT
	ADC EXPADJUST
	STA [D],Y	; store the lo byte
	INY
	LDA EXPONENT+1
	ADC EXPADJUST+1
	STA [D],Y	; store the hi byte
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; StateFin -- replace the updated input pointer INDEX, set	the final value
;; of VALIDPREFIX, and RTS through the return address.
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
StateFin	LDY #$0
	LDA CHARPTR
	STA [INDEX],Y
	LDA FLVALPREF	; store the low byte
	STA [VP],Y
	TYA	; and zero out the hi byte
	INY
	STA [VP],Y
	LDA CHARPTR+1
	sta [INDEX],Y
	rep #$30
	RTS
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; State99 -- all else has failed, so check for INF and NaN.  Use the saved
;; character pointer to back up in case	a partial scan does	not find the
;; full token for a	NaN.  INF is handled at StateINF.
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
State99	LDX charptr
	BNE z15s12
	DEC charptr+1
z15s12	DEC charptr
	JSR GetMapped
	CMP #I	; test for 'I'
	BNE z15s1	;    if not, branch
	JMP StateINF	;    if so, try 'INF'
z15s1	CMP #N	;    else try 'N'
	BNE StateBackup	; if neither, quit looking
	JSR GetMapped
	CMP #BIGA
	BNE StateBackup
	JSR GetMapped
	CMP #N
	BNE StateBackup
;       ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;       ;; NaN found.  Replace N0011 with N4000 as default NaN.
;       ;; {changed	23 May 85  CRL : new sense of signalling NaN bit}
;       ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
	LDY #$2
	LDA #FOUR
	STA [DSIGLOC],Y	; '4' as first digit
	LDA #ZERO	; prepare to zero out the '11' chars
	INY
	INY
	STA [DSIGLOC],Y
	INY
	STA [DSIGLOC],Y
;       ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;       ;; Expect string (xxxx) to follow NaN.  Check for it and
;       ;; translate to a two digit ascii code in hex.  Save the pointer
;       ;; at NAN in case junk follows.
;       ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
	JSR UpdatePtr	; advance pointer to NAN
	JSR GetChar
	CMP #OPENPAREN
	BNE StateBackup	; if no '(' then leave
z15s49	JSR GetChar
	BCC z15s57	; < '0' so check for ')'
	CMP #COLON	; > '9' so junk
	BCS StateBackup
	AND #$0F	; mask to 0..9
	STA STMP	;    and hold digit	until later
;          ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;          ;; Multiply NaNCODE by 10 and add in next digit found.
;          ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
	ASL NaNCODE	; * 2
	LDA NaNCODE
	ASL A
	ASL A	; * 8
	CLC
	ADC NaNCODE	; sum to get NaNCODE * 10
	CLC
	ADC STMP	; add in next digit
	STA NaNCODE
	JMP z15s49	; and keep looking
;       ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;       ;; End of numeric scan.  Now look for the final ')'.
;       ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
	JSR GetChar
z15s57	CMP #CLOSEPAREN	; end with  ')'?
	BNE StateBackup	; if no ')', back up pointer
;       ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;       ;; Process NaNCODE and put two ascii hex characters	in d.sig
;       ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
	LDA NaNCODE
	LSR A
	LSR A
	LSR A
	LSR A
	JSR NibOut	; process hi byte
	LDY #$4	; {CRL19jun85} PUT NAN CODE IN RIGHT PLACE
	STA [DSIGLOC],Y	; and store the ascii
	LDA NaNCODE
	AND #$0F
	JSR NibOut	; process lo byte
	INY
	STA [DSIGLOC],Y	; and store the ascii
	BNE INF3	; always
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; StateBackup -- if the string has been overscanned, then SAVEDCHARPTR
;; contains the index of the last valid	substring.  In this	case, revert to
;; the saved pointer and then branch to	the output routine.
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
StateBackup	LDX SAVEDCHARPTR
	STX CHARPTR
	LDX SAVEDCHARPTR+1
	STX CHARPTR+1
	JMP StNum2	; always
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; StateINF -- look	for simple string 'INF'.
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
StateINF	JSR GetMapped
	CMP #N
	BNE StateBackup
	JSR GetMapped
	CMP #F
	BNE StateBackup
	LDY #$0
	LDA #$1
	STA [DSIGLOC],Y	; length byte of 1
	LDA #I	; put I into d.sig
	INY
	STA [DSIGLOC],Y
INF3	JSR ClearSavedPtr	; use current pointer
	JSR CheckEndScan	; end of scan?
	BCS z16s7	;    if not, branch
	INC FLVALPREF	;    if so, set flag
z16s7	JMP StateFin
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; GetMapped -- fetches next input character, maps to upper	case.  Note
;; that upper and lower case letters differ only in bit #5.
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
GetMapped	LDA #$4	; stack adjust in GetChar
	JSR GetChar2
	CMP #SMALLa
	BCC z17s1	; too small for lower case
	CMP #zPLUS1	; one past 'z'
	BCS z17s1	; too big for lower	case
	AND #$0DF	; mask 1101 1111 for bit 5
z17s1	RTS
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; NibOut -- passed	a nibble in the accumulator, construct an ascii
;; character of it.
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
NibOut	CMP #$0a	;<klh 18oct85>
	BCC z18s1	; 0..9
	ADC #$6	; A..F offset is $37 (6 + carry)
z18s1	ADC #$30	; 0..9 offset is $30
	RTS
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; The subroutines which control the current value of the scan pointer
;;      and the current value of its backup which is used after overscanning.
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
ClearSavedPtr	LDX #$0FF
	STX SAVEDCHARPTR
	STX SAVEDCHARPTR+1
	RTS
UpdatePtr	LDX CHARPTR
	STX SAVEDCHARPTR
	LDX CHARPTR+1
	STX SAVEDCHARPTR+1
	RTS
CheckEndScan	LDX CSTRING
	BEQ z19s3
	LDX LASTCHARPTR
	CPX CHARPTR
;      ;JMP     $5
	rts	; <klh 18oct85>
z19s3	SEC
	LDY CHARPTR
	LDA [thisS],Y
	BNE z19s5
	CLC
z19s5	RTS
	EXPORT SNaN
SNaN	DC.B $00,$00,$00,$00,$05,$4E,$30,$30,$31,$31 ;{SNaN bit change 23May85 CRL}
	longa on
	longi on
	ENDP



	END

