MODULE Comments;
(* This is a simple comment *)
VAR
    a, b: INTEGER; (* Variables for testing *)
          
BEGIN
    (* Assignment statements *)
    a := 10;  (* Set a to 10 *)
    b := a + 10; (* Add 10 to a and store in b *)
    (* Nested comment test: (* inner comment *) *)
END Comments.